# ChatPanel 1:1 复刻 —— 用户反馈跟踪表

> **这张表的用途**:会话上下文会被压缩,压缩后我会忘掉用户口头提过的问题。
> 所以用户反馈的每一条**当场落在这里**,做完一条当场回来打勾。
> 交接时只看这张表就够,不依赖任何人还记得当时的对话。
>
> 状态:`[ ]` 未做 / `[~]` 部分做了(必须写清还差什么) / `[x]` 做完且已验证
>
> 权威源:交付稿 `docs/design/chat-matrix/matrix-82.html`(84 格实体)。
> 验收面:`docs/design/chat-mirror/compare.html`(逐格左右对照 + 逐属性状态章)。

## A. 已完成

| # | 用户原话 / 指的地方 | 真因 | 落点 |
|---|---|---|---|
| A1 | 「这些时间怎么位置跟设计稿差那么多」 | 比对工具的属性表里**根本没有 `margin-left/right`**,而「把秒数推到最右」正是 `margin-left:auto` —— 整类差异从来没被量到 | `diff-cells.mjs` `PROPS` 摊成完整盒模型 |
| A2 | 「下拉箭头位置也不对」 | `.fold.flat summary > .chev` 用了子代组合子,稿子是后代;且漏了 `.meta` | `record.module.css` |
| A3 | 「这缩进感觉完全不对」(#11) | 同上一类的层叠错位 | `record.module.css` |
| A4 | 「这俩啥意思」(#23/#24 share/detail) | 已答复的摘要里把枚举原值直接吐出来了,没走 `readable()` | `QuestionForm.tsx` |
| A5 | 「#41 也没对齐」 | —— | `record.module.css` |
| A6 | 「hover 的前后顺序不对」(#51) | 我按稿子的**注释文字**排(时间在最右),稿子的**渲染**是 时间→复制→重试。以渲染为准 | `AssistantFooter` |
| A7 | 「是点击行为没做还是 hover 眼睛也没做」(#55) | 两个都没做 | `.msg-att-eye` + 点击打开 |
| A8 | 「文档里那么多『本实现出不来这一态』,你 mock 不行吗」 | 我拿「拿不到数据」当借口 | 各态夹具补齐 |
| A9 | 「你所有的都要有截图」 | 22 格空着 | 84 格里 82 格有图(#70/#71 是你拍板不做的 Plan 卡) |
| A10 | 「正文取词你是一点没写??」 | 整个功能没实现 | `QuoteBar` + `QuotedRefs` + `quote-selection.ts` |
| A11 | 「那你做啊大哥!!没有就不做了?」(#78) | 同上 | `SupportDialog` |
| A12 | 「这个线好像也没有」(步骤连接线) | 稿子那两条 `::before` 竖线我一条没搬 | `record.module.css` |
| A13 | 「这出现在运行结束后」(#75/#76) | 我做成了弹窗,产品裁决是流水里的卡 | `UpgradeCard` |
| A14 | 「#72 队列完全不一样」 | —— | `chat.css` 队列行 |
| A15 | 「#9 球」 | —— | `Orb` |
| A16 | 「#11 重试为啥不接?」 | 生图重试按钮只画不点 | `onRetryImage` 接到发送路径 |
| A17 | 「不在这一轮授权内」(报错卡不抽组件) | 这是借口,不成立 | `RunErrorCard` 抽出 |

## B. 待办(当前批)

| # | 用户原话 / 指的地方 | 现象 | 状态 |
|---|---|---|---|
| B1 | 「你间距检查了???」 | 比对工具比的是**属性值**,不是**位置**。两边属性可以条条相同而元素落在不同 y 上 —— 「间距」这类问题结构上量不到 | `[x]` `diff-cells.mjs` 加了**逐位置比对**(相对格根的 x/y/w/h,容差 1px),`compare.html` 的状态章现在两项都报。配套修了两个假因:①两张页面的**栏宽不同**(稿子 440 / 我们 760),百分比宽度天生对不上 —— 陈列页展位已对齐 440;②整个被推到画布外的读屏文本/提示气泡不参与比对(稿子推 -513、我们推 -29,屏幕上都不占像素)。**首次量到:位置差 778 处**,而逐属性只有 359 —— 用户一直说的「间距不对」正是这一半 |
| B2 | 「#90 间隔这么大」 | 工具行(读取 首页.png / tokens.css …)的行距明显大于稿子 | `[~]` 已定位到三类真因,还没清完:**①行高**:稿子的紧凑行(`.tool` 等)是 `--lh-row: 1.5`,summary 那一行反而继承 **1.7** —— 两种混着用,我们有几处给反了(实测:稿 summary 20.4px / `.ms` 18px);**②缩进**:稿子有**两条**嵌套缩进规则(通用 33px + 扁平壳 29px),我原来只搬了 29 那条,非扁平那一路整列少缩 33px(已补);**③队列动作按钮**宽 22 vs 16(见 B9 的行高账)。剩余位置差 778 处,逐格清 |
| B3 | 「#21 箭头指向的都是有问题的」 | 四处:①右上切换图标字形不对(稿子是 2×2 圆点,我们是 ⊞)②右翻页箭头旁多出一块 ③「随机」没和「下一步」同行 ④左下应是「换一批」,我们是「跳过 · 你来判断」 | `[x]` **根因是夹具照错了组件**:我拿 `direction-cards`(模型现给的固定四张,天生没有下一批)去照稿子那张「内置风格目录」卡(prototype 档 26 张),所以左下落回通用的「跳过 · 你来判断」、底栏也只剩「随机」。换回目录那一路后:①切换图标改用稿子的**四个圆角方块**(新增 `grid-4`;原来 `grid` 映射到 remix `grid-line`,画出来是「⊞」)②③④ 底栏合成**一行** `换一批 | 撑开 | 随机 | +22 | 下一步` —— 做法是把「下一步」交给选择器那一行(portal 在 `renderToStaticMarkup` 下不渲染,验收页会照出空插槽)。逐属性从 12 处降到 **4 处**,剩的 4 处是预览底色:稿子是纯灰占位(它没有真图),我们是真预览图 —— 稿子自己的注释也写着「真图到位后换掉」 |
| B4 | 「#40 这为啥又不对齐设计稿??」 | ① i18n key 裸露在界面上(`assistant.feedbackReasonTitle` / `…Placeholder` / `…Submit`)②复选框应为胶囊组 ③缺「取消」④多出设计稿没有的标题表情与 Discord 引流行 ⑤补充框应为「无框 + 一条底线」而非 textarea 方框 ⑥标题应是「哪里不对?」 | `[x]` 胶囊组 / 常驻「无框+底线」补充框 / 取消+提交 / 新键 `assistant.feedbackReasonTitleNegative`(19 语言包已补)。裸 key 的**真因是陈列页的 `t` 桩** `(k)=>k`,已换成真 zh-CN 译器。标题表情与 Discord 引流行已按最新裁决移除,不再保留设计稿之外的附加界面。**原因 code 契约保持兼容**,本次只收敛展示结构与样式。 |
| B5 | 「#49 间隔这么大」 | ① 气泡到「重试」行之间空了一大截 ② `chat.record.retry` 裸 key | `[x]` 裸 key 见 B4(陈列页 `t` 桩)。间距的真因是**结构错了**:稿子的「重试」是 `.msg-act .keep`,**长在动作行里**和时间/复制并排;我另起了一行 `.msg-fail` 挂在气泡下面,于是那条 `opacity:0` 的动作行照样占 30px,气泡到「重试」之间空出一大截。改成:动作行整行常驻,**只淡出非 keep 的子元素**(照抄稿子的 `.msg-act button:not(.keep) { opacity: 0 }`),「重试」进行内 |
| B6 | 「compare 这为啥半边灰色的」(#2 格) | 对照页上格子被一块灰蒙住半边 | `[x]` **真因是 `SupportDialog` 的遮罩**:`inline` 模式原来只跳过 portal,遮罩仍是 `position: fixed`,于是它铺满整张陈列页、把**每一格**都糊上一层灰 —— 之前所有截图都是隔着这层灰看的。稿子的 `.ovl` 本来就不是 fixed(带圆角内距的在流盒子),已照搬成 `.overlayInline`。顺带修了拍照:原来靠 `captureBeyondViewport` 够视口外内容,headless 会吐出没光栅化的灰块;改成**逐格把视口调到这一格高度再滚进视口拍** |
| B7 | 「#50 这跟设计稿也不一样呢」 | ① `chat.record.retry` 裸 key(同 B5②)② 我在格子底下记了「稿子底色其实是**变浅**不是加深,已记待确认」—— 这又是一条尾巴,稿子怎么画就怎么做 | `[x]` ①同 B5 ②不再写「待确认」:稿子的状态名说「背景加深」,它自己的 CSS 是 `--bub-bg: var(--text-strong)` → `var(--text)`,白底上**是变浅** —— 名字和值打架时以**值**为准(值才是能渲染的那一个),说明已改成陈述事实 |
| B8 | 「#65/#66 这按钮跟划线的没对齐」 | 正文取词浮条**没跟着选区走**:稿子里浮条居中压在被划线的那几个字上方(#66 是选区贴顶,浮条翻到下方并左对齐选区);我们的浮条固定在右侧,和划线位置无关 | `[x]` 陈列页原来拿**整个容器**的 50% 摆浮条,和划线那段无关;改成照稿子的做法 —— 高亮段 `position: relative`,浮条绝对定位居中在它上面。顺带修了一个真 bug:`QuoteBarView` 根节点是 `<div>`,摆进 `<p>` 里会被浏览器**当场截断 `<p>`**、DOM 重排后浮条整个不见 —— 已按稿子的 `.selbar` 改成 `<span>` + 显式 `display: block` |
| B9 | 「#73 icon 样式都不对」 | ① 左侧拖拽把手:稿子是 2×3 六点,我们是菱形四点 ② 右侧三枚:稿子是 魔法棒 / 垃圾桶 / 回弹箭头,我们是 铅笔 / 上箭头 / 垃圾桶 —— **图形和顺序都不对** ③ 我自己在格底下记过 🐛「每行错位同第 72 格,行高 34→38px,『3.5 行 = 122px』这笔账算不平」,一直没修 | `[x]` 手柄真因:`Icon.tsx` 把 `grip-vertical` **映射到了 remix 的 `drag-move-line`**(四向箭头菱形),本地那段六点 case 一直是死代码 —— 去掉映射并把坐标对到稿子(cx 9/15,cy 6/12/18,r 1.5)。编辑换成稿子的**魔杖**(新增 `magic`,路径逐字节照抄),顺序改成 编辑 → 移除 → 第三颗。**第三颗当时仍是「立即发送」**:稿子那颗叫「引导对话」(塞进正在跑的这一轮),我们那颗会**打断**再发,是另一件事 —— 能力已在 B11 补齐,现在能引导时这颗就是「引导对话」,引导不了才退回「立即发送」 |
| B10 | 「#78 icon 呢」 | 报错卡三颗按钮**全缺图标**:稿子是 耳机(联系支持)/ 上传(导出日志)/ 循环箭头(从失败处重试);标题那枚也不一样(稿子是**八角形惊叹号**填充,我们是三角警告)。另外我在格底下记的「⚠️ 差异清单…②动作集合待产品定」也是尾巴 | `[x]` 标题图标换成稿子的**实心八边形+感叹号**(原来是三角警告);三颗动作补上图标:耳机(新增 `headset`)/ 上传(`upload`,与稿子同路径)/ 循环箭头(`refresh`,与稿子同路径) |
| B11 | (由 B9 引出)稿子队列行第三颗是「**引导对话**」—— 把这条塞进**正在跑**的那一轮 | 我们没有这条能力。daemon 侧有 mid-turn stdin 注入的底子(`promptInputFormat: 'stream-json'`,只 Claude 有),但要端到端就得配齐 **HTTP 端点 + 契约 + `od` 子命令**(仓库的 UI/CLI 双轨规矩),不是样式改动 | `[x]` 三件套一次落齐:`POST /api/runs/:id/steer`(契约 `ChatRunSteerRequest/Response`,拒绝码 `RUN_STEERING_UNSUPPORTED` / `RUN_STEERING_CLOSED`)+ 队列行第三颗按钮 + `od run steer <runId> [--message | --prompt-file <path|->] [--json]`。能不能引导由 `promptInputFormat` 说了算(`agentSupportsMidTurnSteering`,契约里一处):**接不住的 agent 那颗连名字一起退回「立即发送」**,不用引导的名字干打断重发的事;带附件的那一行也单独退回(引导只走一帧纯文本)。`stop_reason: 'tool_use'` 那一档 stdin 还开着,是能引导的 —— 也正是最该引导的时刻。注入的消息由 daemon 以 `role: 'user'` 落进同一个会话(排在被引导的那条 assistant 之后,就是用户真正说这句话的时刻),刷新后还在。**图标仍是 `arrow-up`**:稿子那枚「回弹箭头」的具体路径没随稿进仓,B9 已就图标单独结过案,这一轮只动语义不猜字形 |
| B12 | (自查发现)`RunErrorCard` **抽出来了却没接回产品** | 产品里报错走的还是 `UserActionCard`(说明藏在「查看详情」折叠里),只有陈列页在用 `RunErrorCard` —— 那一格照的是个没人用的组件。稿子的 `errb` 是说明**直接可见**,这是行为改动不只是样式 | `[x]` 产品的报错面从 `UserActionCard` 换成 `RunErrorCard`:说明**直接可见**(稿子的 errb 就是一句话),诊断原文仍收在折叠里(那段是给工程看的)。`data-user-action-card` / `data-user-action-footer` 两个测试与 e2e 的稳定钩子原样保留 |
| B13 | (自查发现)发送失败态**在产品里没有人产生** | 契约有 `ChatMessage.sendFailed`、UI 画得出来,但没有任何代码把它置上;`onResend` 也没有上游传进来。第 49 / 50 格照的是一个**还没接线**的状态。接线点在 `ProjectView` 的 `streamViaDaemon` `onError`:当**从来没建出 run**(`!prev.runId` 且非 api 模式)时,应把用户消息标成 `sendFailed` 并撤掉 assistant 占位。**要先有红测**再动 —— 那是 run 生命周期最容易出事的分支 | `[x]` daemon 在建出 `runId` 前失败时撤掉虚假的 assistant 占位、把原用户消息持久化为 `sendFailed`;重试复用同一用户消息 ID 和原上下文/附件元数据,预检未启动会恢复失败态。已有 `runId` 的 assistant 失败路径不变。组件接线与 ProjectView 生命周期红测均已补齐。 |
| B14 | (量位置时发现)**清单之前那段叙述在产品里丢了** | 稿子第 2 格的扁平壳里第一个孩子是 `<p class="think">两张图是同一套栅格…`;我们渲染不出来。夹具里那条 `thinking` 事件是有的,`build-turn-blocks` 把它 `pushInside` 到了当时的 sink,随后被清单接管时丢掉(D36 那条 sink 规则)。**要动落块规则,得先补红测** | `[x]` **被落块裁决顺带解决**:那段 thinking 现在稳稳在第一张壳里(第二张是清单),正是裁决要的形态 |
| B15 | (同上)**每条 todo 的耗时没渲染** | 稿子每个 todo 抽屉的 summary 右侧有 `.ms`(如 `18.2s`);`TodoRow` 有 `formatElapsed(segment.elapsedMs)` 的分支,但 `elapsedMs` 从来没被算出来 | `[x]` 新增 `segSpan`:事件发生时哪条 todo 在跑,这段时间就算在它头上;轮末落到 `segment.elapsedMs`。红测先行 |
| B16 | 「这个时间怎么跑到右边去了,然后文件名变成…了」 | 工具行里长文件名被压成 `...`,耗时被甩到最右。稿子 `.tool .ms` 只有 `flex: none`,**不带 auto 外距**;我们的 `.meta` 基底一律 `margin-left: auto`,只在 `.fold.flat summary` 里清了 —— 工具行没清到 | `[x]` **与 B20 同一件事**(你先后指了两次),真因与修法见 B20:`elideFileName` 保后缀中间截断 |
| B17 | 「这个缩进不对吧?」(#6 终端块) | 稿子里代码/终端块的左缘**比行文字更靠左**(`.fold.mod-flat > .body.mod-stack > .code { margin-inline: 22px 11px }`、`.fold.mod-flat .body.mod-stack .code { margin-inline: 29px 18px }`、`.fold.mod-flat .body.mod-stack .body.mod-stack > .code { padding-inline-start: 0 }`);我们的块缩进跟行文字齐平 | `[x]` 我们的 `.code` 免缩进规则少写一个 `.flat`,只有 (0,4,0),压不过新加的 `> *` 的 (0,5,0),块跟着吃了 29px。选择器补齐 |
| B18 | 「#7 这个缩进怎么这么深啊?」 | **我今天引入的回归**:补 `.body.stack .body.stack > *` 那半之后,嵌套抽屉的 `.fold` 盒子和它的 `summary` **各吃了一份 29px**,叠成 58。稿子有一条 `.fold.mod-flat .body.mod-stack .body.mod-stack > .fold { padding-inline: 0 }` 专门把盒子那份清掉 —— 我没搬 | `[x]` 补上稿子的 `.fold.mod-flat .body.mod-stack .body.mod-stack > .fold { padding-inline: 0 }` —— 缩进只由 summary 承担,盒子不再各吃一份 |
| B19 | 「#21 这个边距问题?」+「这个 +22 看看」 | 底栏在 440px 宽下**撑破卡片**:「换一批」被左边界切掉、「下一步」贴右边界。多出来的那颗 **`+22`** 是稿子没有的 —— 稿子右上角那枚四方块图标就是「摊开看全部」的入口,底栏只有 `换一批 / 随机 / 下一步` | `[~]` **边距已修**(底栏丢了卡片底栏的 `padding: 0 11px 8px`,整行贴死左右边界)。**`+22` 待办**:按你的裁决改成「网格视图摊开全部」,然后删掉 `+22` |
| B20 | 「#4 时间跑到右边、文件名变成…」 | **真因**:稿子那一行在源文件里就是截好的字符串(`设置页-…-f….png`,后缀完整);我们靠 CSS `text-overflow` 截,而文件名包在 `<button><code>` 里对 flex 是**一个原子块** —— 要么整块放下要么整块被省略号顶掉,于是只剩「…」、后缀没了、耗时被推到行尾。`FileButton` 的注释里我原来写着「阈值和量法等设计答复」,又是一条尾巴 | `[x]` 新增纯函数 `elideFileName`(预算 28 = 后缀 4 + 省略号 1 + 主名 23,由稿子那一行反推),**红测先行**(5 条),读屏与 tooltip 给全名 |
| B21 | 「#50 重试 icon 也不对」 | 稿子那一枚是**循环箭头**,我放的是实心感叹号 —— 感叹号说「出事了」,这颗按钮说「再来一次」 | `[x]` 换成 `Icon name="refresh"`(和稿子同一条路径) |
| B22 | 「动画 icon 和右边『进行中』的文字上下没对齐,也没什么间距」(真实客户端) | **陈列页照不出的一类**:球是 `<canvas>`,静态渲染时是空白,所以它的**真实排布我从来没验过**。真因:壳头把 `.mark`(15×15 状态点圆)挂给了球 —— 24 的画布被压进 15 的格子,而且 canvas 默认 **inline**、吃基线、底下垫出行内下沉空间。稿子另有一套 `.orb`(`inline-flex` + `align-items:center` + `canvas{display:block}` + 按尺寸档的负外边距) | `[x]` 照搬稿子的 `.orb` 一族,`Orb` 补 `data-orb-box`,壳头改用 `.orb` |
| B23 | 「问句的自由填写框换成设计稿那种一条底线的」 | 意图澄清里「自己填 / 请提供内容」那个输入框现在是**四边框 + 大方块**;稿子是**无框 + 一条底线**(同第 40 格反馈面板那条 `.ta` 的做法) | `[x]` 改成稿子的「无框 + 一条底线」(和 `.qf-custom .qf-input` 同一种做法),`field-sizing: content` 跟着内容长高 |
| B24 | 「左下角的跳过按钮样式不太对」(分步态) | 分步底栏的「跳过」字重/颜色偏重,而且贴着卡片左边界 —— 稿子是 `.btn.mod-ghost.mod-sm` 走 `--text-muted`,并且 `.foot > .btn:first-child { padding-inline: 0 }` 让它落在卡片的 11px 竖线上 | `[x]` 两处:①`.question-form-foot > button:first-child` 改成 `:first-of-type` —— 分步态下前面有一枚倒计时 `<span>`,按 `:first-child` 匹配不到「跳过」;②倒计时不再带 `flex: 1` 把「跳过」挤到右边 |
| B25 | 「已回答这些问题」那张卡「怎么没变成最新的设计稿样式」 | 产品还是**灰底圆角卡 + ✓ 圆圈 + 胶囊对**;稿子第 23/24/25 格是**绿色「已确认」+ 纯行式 `标签 值`**,视觉方向那格还带选中的那张缩略图。`AnsweredSummary` 没按稿子重做 | `[x]` **真因是有两份实现**:`QuestionForm` 里的 `AnsweredSummary` 早就按稿子写好了,但产品历史回放走的是 `AssistantMessage` 里另一份 `question-form-summary`(灰卡 + ✓ 圆圈 + 胶囊)。已把后者改成同一套 `.answered / .k / .ab / .ak / .al / .av` |
| B26 | 「思考中 + 时间」那一行重复了 | 壳头已经在报「进行中」,底下的老回合状态行又报一次「思考中」。现有抑制开关 `hideRunStatus = recordHasContent \|\| hasTodoSnapshot \|\| 报错卡` —— 壳**刚建出来还没内容**时三条都不成立,于是两行同时在 | `[x]` 与 B31 / B36 同一条规则:**运行中整行不出**(壳头已经在报状态),跑完才出并报终态 |
| B27 | 「上面的 title 是啥东西?」 | `<od-title>…</od-title>` 原样漏进正文。**真因**:调用点写的 `enabled: Boolean(titleGenerationRequested)` —— 不请求标题就把剥离器整个关掉;而模型是按系统提示词吐这个标记的,它不知道这一轮我们要不要标题 | `[x]` 语义拆开:**永远剥离**,只有请求过标题才上报。红测先行,并删掉了与之直接矛盾的旧规格 |
| B33 | 「这些是啥啊?」(`<CRITIQUE_RUN>` / `<PANELIST>` / `<ROUND_END>` 整块漏进正文) | 评审团(critique)的通信协议原样打给了用户。**根因方向**:`apps/daemon/src/*.ts` 的可见文本路径上**根本没有 panel 语法的剥离器**(`<od-title>` 有,critique 没有)—— 一旦提示词注入了面板语法而编排器没接管这条流,协议就直接进正文。要定位是哪道门失灵,得拿这一次 run 的事件流看。属 `apps/daemon/src/critique/`(有自己的 AGENTS.md) | `[x]` **已修并在真机验过**:语法搬到 `@open-design/contracts`(`stripCritiqueGrammar`)作为唯一出处 —— daemon 用它剥**流**(带半截缓冲,不闪),web 在 `AssistantMessage` 的 `cleaned` 里用它剥**历史**。旧对话不能因为「以后不会再有了」就烂在那儿:真机上那条消息原来有 **16 处**标记,修完 **0 处**,标记之间那几句人话原样留着。剥的是壳不是字(`<PANELIST role="Critic" score="9.0">已完成…</PANELIST>` → 「已完成…」)。红测先行,撤掉实现复验会红
| B28 | 「下面不是有 5 步吗?怎么后四步没显示?」 | 「执行计划 · 5 步」在,但壳里只列出了第 1 条 todo 抽屉,后 4 条没出 | `[x]` **真因**:落块时写着「`status !== pending` 才推成行」,还没开始的那几条连行都不建。「说好几步」和「看得见几步」必须同一个数。红测先行 |
| B29 | 「上面已完成的怎么还在倒计时」 | 已经「已完成」的壳,秒数还跟着下面那个「进行中」的壳一起往前走(两个都显示 1m 37s 并同步递增)。跑完的壳该把秒数**定死在结束那一刻** | `[x]` 耗时改成**按壳自己**算(每张壳记自己的起止),只有还在跑的那张跟着 now 走。红测先行 |
| B30 | 「这个命令没什么可读性呢」 | 工具行显示成 `执行 node - <<'NODE'` —— heredoc 被原样截断,读不出这条命令在干什么 | `[~]` 先修了 heredoc:新增 `commandHeadline`,把 `<<EOF` 这类开启标记和其后孤立的 `-` 去掉(`node - <<NODE` → `node`)。**更根本的健壮性正在调研**(已派 subagent 捞真实语料 + 压测现有正则 + 对比开源方案) |
| B31 | 「运行中没有这个了,干掉」 | 跑的过程中底部还挂着一条「复制 + 16:57」的回合状态行;稿子里这一行是**跑完之后**才出的 | `[x]` `showCompletionRow` 加上 `!streaming` —— 回合状态行是**跑完之后**才出的一行 |
| B32 | 「本轮产出的文件」这块「也没这个了,变成上面卡片形式才对」 | 产物文件现在是**列表行**(名字 + 体积 + 打开 / 下载);应该走上面那种**产物卡**形式 | `[x]` 新增 `doc` 档产物卡:拿不出预览图的产出(md/txt 等)也走同一张卡的骨架,缩略图位换成「图标 + 文件名」。稿子没画这一档 —— 它每张卡都假设有缩略图 |
| B34 | (我引入的回归,你当场抓到)`执行 wc -l brand-spec.md tr….html` | `elideFileName` 被套到了**命令字符串**上,末尾的 `.html` 被当成「后缀」保住,读起来像另一条命令 | `[x]` 省略改成由调用方按语义开(`FileButton` 的 `elide`),只有真文件名开;加了回归守卫测试 |
| B35 | (我发现并口头说过,之前漏记)反馈面板的 Discord 那句是**硬编码英文** | 整个面板 19 种语言都翻了,只有这句没有 i18n 键,英文用户之外都会看到一句突兀的英文 | `[x]` 两句都进语言包(19 个),按 `{discord}` 占位切开再塞链接 —— 占位在各语言里的位置不同,不能拼字符串 |
| B36 | 「这个状态你好像也丢了」(跑完后的「✓ 已完成」) | 回合状态行跑完之后该显示**绿勾 + 已完成**;现在因为 `hideRunStatus = recordHasContent \|\| …`,只要壳里有内容就把整个状态词藏掉,于是跑完也不出 | `[x]` `hideRunStatus` 收窄成只在报错卡那一轮让位;运行中的去重改由整行不出来解决 |
| B37 | 「compare 里看起来都实现了,为什么本地跑起来还是旧的?你的 compare 是假的吗?」 | **不是假的,但它证明的比我以为的少**。①它只覆盖**我喂了夹具的那条分支** —— 你截到的灰色气泡是 `.msg-applied-context`(消息带的上下文),我在陈列页给 `UserMessageImpl` 传的是 `appliedContextItems={[]}`,这条分支**从没被照过**,84 格交付稿里也没有它的格子;②它是**静态渲染**,canvas / 逐字动画一律空白 —— 球和文字没对齐能活到今天就是因为这个。**结论:绿章 ≠ 产品里长这样** | `[x]` **已交付**(另一位 agent,`c4936bfc06`):84 格之后新开「端到端 · 真实形态回归」5 格,每格喂一整条真实会话(用户消息 → 助手消息 → 回合状态行),按产品自己的祖先链渲染。覆盖 84 格照不到的分支:`appliedContextItems` 灰色上下文行、`runContext.workspaceItems` 芯片、`producedFiles` 三种卡混排、一轮两张壳、失败轮 + 报错卡,外加 done 密钥协议。紫色区段与 84 格明确分开,下游 `build-compare.mjs` / `diff-cells.mjs` 解不到它们
| B38 | 「把这个东西干掉」(消息上方那枚「⚙ 设计」小徽标) | 用户消息上方的上下文行里,那枚「设计」类型徽标要去掉;右边「正在使用 Kami Parchment Document」那行保留 | `[x]` 消息上方那一行不再挂会话模式徽标(组件保留,别处可能还要用) |
| B39 | 「+22 别出现了,左右切换的卡片列表直接渲染 22 个不就行了」 | 视觉方向选择器**不再分页**:扇形/网格直接装下整份目录(prototype 档 26 张),`+22` 这颗按钮删掉。稿子只画 4 张是**模拟数据**,不是规格(见 C 节) | `[x]` 已做:`QuestionForm` 的 `compactCards` 改成**整份目录都进这一沓**(`Array.from({length: cards.length})`),分页取消,「+22」那颗按钮随之消失。`offset` 保留 —— 「换一批」现在是把这一沓整体转过去,不再是「换到下一页」 |
| B40 | 「卡片里没有内容只有纯色」 | 视觉方向卡的预览图没加载出来,只剩底色 —— 目录的预览图走远程资源地址,本地跑时多半 404 | `[x]` **已解**:2026-08-26 用户自己的截图里,视觉方向卡的预览面是**真图**(暖白画布、墨蓝、羊皮纸那几张),不是纯色 —— 这一条随目录接管 `direction-cards` 一起消失了(见 D-catalog 节)。
原来那四张纯色卡是**模型现开**的 `direction-cards`,它没有素材、只能画占位块;现在这类题一律由内置目录接管,每张都带 R2 上的真预览图。
排查过程中证伪的三条假设留档,免得下次再追:①目录 **96 张**预览图逐个探过,非 200 的 **0** 张;②真实客户端 `new Image()` 拉那个域秒回 1600×1200,不是 CSP;③CSS 画的降级骨架也都在。
**方法教训**:我一度在陈列页量到「52 张图全 pending、一张不 error」差点当成 bug —— 真因是**那个标签页在后台**,Chrome 会推迟 `loading="lazy"`。量 lazy 图必须让标签页在前台。
①**不是资源缺失** —— 目录里全部 **96 张**预览图逐个探过,**非 200 的 0 张**(deck 25 / prototype 26 / document 11 / image 22 / video 12);
②**不是 CSP / 网络** —— 在真实客户端页面里 `new Image()` 拉 `repo-assets.open-design.ai` 的图,秒回 1600×1200;
③**不是降级样式丢了** —— `.qf-visual-preview-prototype` / `.qf-preview-app` 那套 CSS 画的骨架都在。
顺带记一条**方法教训**:我一度在陈列页量到「52 张图全 pending、一张不 error」,差点当成 bug ——
真因是**那个标签页在后台**,Chrome 会推迟 `loading="lazy"` 的图;切到前台立刻开始加载。
**量 lazy 图必须让标签页在前台**,否则读到的是假的「图坏了」。
**下一步**:需要真机上复现出那一屏(起一轮 discovery 让视觉方向卡出来),不能再靠代码推测。
| B41 | 「左右边距怎么还没改?你是还没截新的图是吗?」 | 我改完之后**没有重新拍 compare 的图**,对照页上还是旧截图 | `[ ]` 这一批全部改完统一重拍 |
| B42 | (落块裁决的连带,我自查发现)`<done/>` 两侧的正文**粘成了一段** | 裁决把「没有 todo 时的正文」挪到壳外之后,过程叙述和结论都在壳外,而渲染层把相邻的 prose 合并了 —— 屏幕上是一整段。标记本该仍然分段:两者不是一回事 | `[x]` **被落块最终裁决顺带解决**:裁决之后 done **之前**的正文根本不出壳(全在卡片里),所以「done 两侧的两段壳外正文粘成一段」这个场景不再存在 —— 卡外只可能有 done 之后那一段。`block-attribution-ruling.test.ts` 的「开场白和结论各自成块,不粘成一段」钉住了这一点 |
| B43 | 「运行中的 todo 应该是这个动效,你怎么显示的是这个」 | 步骤级「正在跑」那颗记号:稿子是**绿色带高光的球**(CSS 画的 `.mk.is-run` + `.sheen` / `.rim`),我们放的是 thinking-orbs 的**散点 canvas** | `[x]` 已做:`StatusMark` 的运行态改成稿子那颗 **CSS 画的绿球**(`<span class="mark run"><i class="sheen"/><i class="rim"/></span>`),不再是 thinking-orbs 的散点 canvas。`record.module.css` 里补齐了稿子的 8 层 conic-gradient + `@property --orb-angle` + `orb-spin`/`orb-drift`,并带 `prefers-reduced-motion` 降级。轮次级别用 orb、步骤级别用绿球 —— 这是稿子自己写明的分档 |
| B44 | 「为什么有两个一模一样的 todo」 | **我 B28 那条改出来的回归**:清单一到 `addPlan` 已经把每条推成行,后面状态从 pending 推进时又推一遍;`pickCurrent` 的隐式点亮也推一遍 —— 同一条最多出现三次 | `[x]` 行只在清单落下时推一次,后面**只改状态**(状态是同一个 segment 对象上的字段)。加了回归守卫:每条 todo 在 items 里最多出现一次 |
| B45 | 「计时结束了还是进行中的状态,下面几个 todo 完全没状态」 | 截图是**绿球改动生效前**拍的:那时「正在跑」和「没开始」都画成虚线圈,自然分不出来。现在正在跑的是稿子那颗自转绿球,没开始的仍是虚线圈 | `[x]` **根因不在渲染也不在数据,在去重**。真机事件流里五次 TodoWrite **共用同一个 tool id `item_2`**(这家 agent 把「计划」建模成一个反复改写的条目),而 `dedupeToolUsesById` 按 id 只留第一条 —— 后四次状态推进全被丢掉,UI 永远停在「四条全 pending」那一版,第一条却同时挂着 35.1s 的耗时和「未开始」的记号。落块器本身是对的(单独喂五次快照能算出四条全完成)。修法:新增 `isSnapshotTool`(住在 `tool-kind.ts`,`build-turn-blocks` 的 `TODO_NAME_RE` 并入它,只留一个出处),**快照型工具不参与 id 去重** —— 它每次调用都是状态替换,不是重复投递;重放两遍也无害,落块是原地更新。红测先行(3 条,含「普通工具照旧去重」的反面守卫)。真机复验:四条全绿 + 各自耗时 8.8s / 14.7s / 11.4s
| B46 | 「评审图这个东西干掉吧」(产品裁决) | 评审剧场(Critique Theater)已在**总闸**上关死:skill / project / env / phase 四层一起失效,提示词不再注入、编排器不再启动。**注入源至今没查清** —— 漏出来那一轮跑的是 codex(`json-event-stream`,非 plain),按判据根本不该注入;而可见文本路径上**没有任何 panel 语法的剥离器**。代码没删(`apps/daemon/src/critique/` 有自己的 AGENTS.md 和归属人),整体移除是另一个决定 | `[x]` |
| B47 | 「下拉展开卡片里如果没有工具调用或任何内容,就不显示这个了」 | 空的执行记录壳(只有「已完成」一行、里面什么都没有)整个不该渲染 | `[x]` 已做:落块末尾按「壳里有没有东西」过滤 —— `items.length > 0 || segments.length > 0` 才留;**轮次还在跑时不过滤**(那时空壳就是「进行中」那一行本身)。红测在 `tests/runtime/chat/empty-shell.test.ts` |
| B48 | 「输入框还是没跟设计稿对齐,我之前截图让你改过,你没记到文档?」 | **记了(B23),也改了 `.qf-textarea`,但真机上还是方框** —— 说明这一格走的不是 `.qf-textarea` 那条分支,或者被别的规则压住。这次**用真实浏览器查 DOM**,不再靠猜 | `[x]` **只修了一半**:上次改的是折叠「自己填」那一路(`.qf-custom .qf-input`),而 `type: text / number / textarea` 三种自由填写走的是**裸** `.qf-input` / `.qf-textarea`,那条规则只写了字号,于是掉回浏览器默认的四边框 + 4px 圆角 + 灰底。真机实测确认(`1px solid #dbdbdb` / `radius 4px` / `bg #fafafa`)。已把「无框 + 一条底线」提到 `.qf-input, .qf-textarea` 这一层,`select` 不在内(它有原生下拉指示符,拆边框会看不出可选)。顺带删掉下方两条过期的重复聚焦规则。真机复验:`border: none` / `border-bottom: 1px` / `radius 0` / 透明底
| B49 | 「为什么不一发送消息就立刻显示?第二轮第三轮每次都要等一会儿」 | 「进行中」那张壳要等 agent 的**第一条事件**才出(`ensureShell` 挂在事件上)。D10 写的是「空态先出来,不等任何 agent 信号」—— 发送的那一刻就该有壳 | `[x]` 已做:`if (turnIsLive) ensureShell()` —— 轮次一开始就建壳,不等第一条事件落下来。原来要等真实事件才开壳,所以第二轮第三轮都要「等一会会儿」才出现 |
| B50 | 「不是说运行中时最下面这一行不显示吗?」 | 我只藏了**状态词 + 时间**,保留了复制/Fork —— 因为有一条带单号的能力 `recvqacy887jsF`(流式中途就能复制已出的文字)。你现在**第二次**明确说整行不要,按你的裁决:运行中整行不出,那条能力被产品覆盖 | `[x]` 已做:`showCompletionRow` 前面加了 `!streaming` —— 回合状态行是**跑完之后**才出的一行,运行中整行不出(壳头已经在报状态) |
| B51 | 「这个 warning 也不要显示了」 | 橙色的 `warning`(「Skill descriptions were shortened…」)不该出现在对话里 | `[x]` 已做:`warning` 这一档在对话里直接 `return null`(`AssistantMessage.tsx:1066`)。`error` 档不受影响,仍然出 |
| B52 | 「点击 fork 按钮后应该会有这样一条东西才对」(稿子第 38 格) | 点过「新开会话」后要**原地落一条分界线**,线上写新会话标题,底下一行「上文已带过来了,接着说就行」。契约里 `ChatMessage.forkedInto` 和 `.fork-sep` 样式都建好了,**但没有任何代码把它置上** —— 和 B13 同一类:字段有、UI 有、没人写 | `[x]` **已交付**(另一位 agent,`034d95867e`):分叉时给源会话那条助手消息盖上 `forkedInto`(标题承接原会话,不是新标题),渲染成稿子第 38 格的发丝分界线 + 居中脚注。落库要新列 `messages.forked_into_json`(原来 PUT 上来整个字段被丢掉),已配迁移。19 个语言包补了 `assistant.forkNote`。红测三层(daemon HTTP round-trip / web 容器 / 渲染)。**遗留**:只落在源会话,新会话末尾那条 seeded 副本还没盖(daemon 建会话时重新发号,不是同一条)

## D. 产品裁决 · 落块规则(2026-08-26,用户口述)

> 原话:「这里有两张展开收起卡片,但是第一张卡片那边还没有产生 todo,那这部分收起的应该
> **只有工具调用或 thinking 啥的**才对吧?然后把**普通文本放到外面**。当有了 todo 后,
> 来了第二张卡片,第一个展开收起卡片就收起,但此时**之前那些普通文本还是留在外面的**,
> 然后**有了 todo 后的所有内容都收进当前正在执行中的那个 todo**。」

规则拆解(实现按这个来):

1. **还没有 todo 时**:壳里只收 **工具调用 / thinking**;**普通正文留在壳外**。
2. **第一条 TodoWrite 落下**:另起第二张壳(清单壳),第一张收起。
   **此前已经落在壳外的正文,原地不动**(不回收、不搬家)。
3. **有 todo 之后**:后续**所有**内容(工具调用、thinking、**以及普通正文**)
   都收进**当前正在进行的那条 todo**。

与既有决策的关系:
- 这条**收紧了 D43**(原来是「done 之前的过程叙述画在壳里」)——
  现在「过程叙述」在**没有 todo 的阶段属于壳外**。
- **thinking 不受影响**:它任何阶段都在壳里(与稿子第 2 格的 `<p class="think">` 一致)。
- 待确认的一点:第 1 阶段「普通文本放外面」指的是**模型的正文输出**,
  不含 thinking —— 我按这个理解实现。

### ⚠️ 两条裁决打架(需要你确认我选得对)

- **2026-08-25(T34)**:「创建 todo 时,**上面有文案**才新建一个卡片包裹 todo」
  → 只干活没说话**不分张**。当时给的理由是:分出来两张都写「已完成」、
  **耗时还是同一个数**,读着像同一件事说了两遍。
- **2026-08-26(本次)**:「第一张卡片那边还没产生 todo,那部分收起的应该只有
  **工具调用或 thinking**……当有了 todo 后,**来了第二张卡片**」
  → 第一张装的是工具/thinking,照样分张。

**我按新的实现**(判据从「壳里有正文」换成「壳里有东西」),理由:
① 新的更晚且描述的正是你要的形态;
② 旧裁决的顾虑是「两张耗时同一个数」—— 那是 **B29 这个 bug**,我正在修,
   修完之后第一张会定死在它自己结束的那一刻,不再和第二张同步走。

如果你要的是旧的那条(工具-only 不分张),跟我说,一行判据就能换回去。

## D-final. 落块裁决(2026-08-26 最终版,推翻同日两条中间裁决)

用户原话,分两次给:

> 「没有 todowrite 时,所有工具调用或普通文本或者 thinking,都收拢在展开收起卡片里。
>   当有了 done 信号之后,输出的平台文本内容才会显示到卡片外面,但如果有工具调用啥的,
>   还是会收到卡片里。一旦某一个有了 todowrite,就收起之前的卡片,新开一个卡片里面
>   开始 todo 相关的渲染逻辑和规则。」

> 「如果有了 todowrite,不用第二张卡片,继续第一张卡片里继续输出就行了,因为此时肯定是
>   没 done 信号的…卡片外面也没文本…会出现两张连起来的卡片,不太好。」

### 现行三条

1. **done 之前:一切都在卡片里** —— 工具调用、普通正文、thinking 一律收进当前 sink
   (有 todo 就进当前那条 todo,没有就进壳)。
2. **done 之后:只有正文出卡片**;之后若还有工具调用,仍然收进卡片 —— 它是过程不是回答。
3. **卡片之间的边界由「卡外落过东西」决定,不由清单决定** —— 一轮正常跑完就是**一张**
   过程卡;只有 done 之后 agent 又开新计划继续干时,才在结论下面另起一张。

### 被推翻的两条,以及为什么

- **「还没有 todo 时正文落壳外」**:`ensureShell` 在循环开头就把壳压进了 `blocks`,
  于是开场白排到了**整张卡之后**,还和 done 之后的结论粘成一段。用户真机截图指认:
  「这个话怎么感觉是在创建 todo 之前的?…那么这段话应该在展开收起的卡片之前才对呢?」
- **「一出现 TodoWrite 就收起前一张、新开一张」**:两张卡之间**永远不会有东西隔开** ——
  卡外唯一会出现的内容是 done 之后的结论,而 TodoWrite 必然在 done 之前。所以那条规则的
  产物一定是两张紧贴的卡:两个「已完成 + 秒数」的头,说的却是同一段连续过程。
  它还制造过两张卡头**显示同一个耗时**的坏画面(thinking 事件不带时刻,前一张只能
  退回轮次跨度)—— 陈列页的端到端格照出来过。

### 连带必须做的两件事(不做会把答案埋掉)

- **兜底提结论**:整轮没发 done 时,轮末把当前 sink 里最后一段**回答**提到卡外。
  没有这条,一个「只答话、不调工具、又没发 done」的普通回合会被整段埋进默认收起的抽屉,
  用户看不到自己问题的答案。
- **`ShellText` 加 `thinking` 标记**:兜底**不许提 thinking** —— 提出来就是把
  「想什么」当成「答什么」。踩过一次:整轮只有一句 thinking 时它被提到壳外、
  壳空掉后整张壳被丢,「思考中」那一格直接没了。
  连带:`pushInside` 的合并判据要认「同一种文字」,thinking 和回答挨着时必须分成两段。

### 历史基线

`historical-turn-baseline.json` 已按最终裁决重刷,四处变化逐条核过(见
`done-marker-key.test.ts` 的注释)。其中一处是**修复**落进基线:旧基线里
`bare-done-after-todos-closed` 有一条**重复的 todo 行**(3 行只对应 2 条 segment),
正是用户指认的「为什么有两个一模一样的 todo」—— 上次修它时没有同步重刷基线,
于是这条测试在 HEAD 上**本来就是红的**。基线一旦欠着就从守护栏退化成噪音。

## D-hover. 气泡 hover 不变色(2026-08-26 用户裁决)

> 「不要 hover 变色反正,你干掉吧」

**起因**:用户问「为什么我看原始的设计稿,hover 上去没反应呢?」查下来 ——
交付稿 `docs/design/chat-matrix/matrix-82.html:712` 确实写着
`.msg-row.is-hover .bub, .msg-row:hover .bub { --bub-bg: var(--text) }`,
但那条在**它自己那一页上是死规则**:普通气泡的祖先是 `.msg-me`(全文 7 处),
`.msg-row` 全文只出现 **2 处、且都写死 `is-hover`** —— 那是两格专门摆拍 hover 态的格子
(带「重试」那两格)。也就是说在原稿上普通格子 hover 本来就没有任何反应。

摆拍那两格量出来是 `#494949`,和我们产品 hover 时一模一样 —— 实现没走样,是这条规则
本身该不该存在的问题。用户据此裁掉。

**顺带解掉一条历史矛盾**:那节的状态名写「背景加深」,值却是 `#202020 → #494949`,
白底上是**变浅**。原来按「值优先」实现并挂着待确认,现在整条删掉,矛盾一并消失。

**hover 仍然有反馈**:时间与复制会浮出来(`.user-actions` 那条 `opacity: 0 → 1`),
真机复验过 —— 气泡底色不动,`21:43` 与复制按钮照常出现。

## D-footer. 回合状态行只在最后一轮出(2026-08-26 产品裁决)

> 「我问了一下产品同学,应该只有最后一轮底部才会显示,之前轮次不要显示,hover 也不显示」

**当天改过两次,记清楚免得再来回**:
1. 用户先问「为什么 hover 在这一轮消息后,才会出现下面这一行? 这不是要常驻的吗?」——
   查下来交付稿的 `.fb` 确实**没有任何 opacity / hover 门控**,于是改成常驻;
2. 用户问过产品同学后回来定死:**只有最后一轮出**,历史轮次连 hover 都不出。以这条为准。

**判据放在渲染层**(`AssistantMessage` 的 `showCompletionRow` 加 `!!isLast`),不用 CSS 藏:
原来那份 `opacity: 0` + hover 显形的门控在 `styles/viewer/composio.css` 和
`styles/viewer/routines.css` **各写了一遍**,后者靠 `.app` 拔到 (0,2,0) 且排在 `index.css`
最后 —— 特异性和顺序两头都赢,只删一处不生效。直接不渲染就没有这场层叠仗。
两处 CSS 门控**都已删干净,别再加回来**。

**代价已明说**:复制 / 反馈 / 分叉这几个动作在历史轮次上因此够不着了。这是产品要的取舍;
要给历史轮次留入口得另想落点。相关测试 `chat-feedback.test.tsx` 里那条
「每一轮都有一组反馈控件」已按新口径改写。

红测:`tests/components/AssistantMessage.completion-row-last-only.test.tsx`(3 条,含
「不是最后一轮 → 整行不渲染,所以 hover 也不可能带出来」)。

## D-vnav. 一沓的左右箭头距两侧 11px(不是贴边)

用户圈图反馈「左右边距啥的好像还是不对」。稿子 `.opts.mod-visual .vnav` 是
`inset-inline: 11px`,我们写的是 `inset: 0` —— 箭头压在这一沓的左右边缘、和卡片描边叠在一起。
11px 和「换一批 / 随机」那一行的左右内距是同一个数,三者竖向边界因此对齐。

竖向仍由 `.qf-visual-stage` 承担(它的高度就是 `--qf-fan-h`),**不照搬**稿子的
`bottom: 10px; height: var(--fan-h)` —— 稿子那两条是因为它把 `.vnav` 挂在更高的 `.opts` 上、
要自己把盒子压回这一沓;我们的定位祖先已经正好是这一沓,再写一遍会错开 10px。

## D-catalog. 内置风格目录接管 `direction-cards`(2026-08-26 用户裁决)

> 「它那个 4 个卡片数据是从设计稿里搬过来的,但设计稿是 mock 的…我们原本应该有这个
>   风格选择的功能,而且原本应该也有缩略图啊,我记得当初还是我传到 R2 去的,一大堆图片」
> 「为什么不把 tone 的内容换到 direction-cards 里?」

**三件事分清楚**:
- **真功能是活的**:内置目录挂在 discovery 简报的 `tone` 那道题上,宿主认到
  `q.id === 'tone'` 就把模型给的纯文字选项换成目录卡。共 **96 张**预览图(deck 25 /
  prototype 26 / document 11 / image 22 / video 12),住在 R2
  `repo-assets.open-design.ai/style-catalog/v1/` —— 逐张探过,**非 200 的 0 张**。
- **用户截到的「四张纯色卡」不是这条路**:那是模型**自己现开**的 `direction-cards`
  (提示词允许它在「用户明确要看视觉方向」时发)。模型现开的卡**没有素材**,
  预览面只能画占位块。
- **一处死代码**:`renderDirectionFormBody()`(5 个硬编码方向、调色板色块、无图)
  在 `apps/daemon/src/prompts/directions.ts` 与 `packages/contracts/src/prompts/directions.ts`
  **各有一份,全仓零调用点**。这才是真正「从设计稿 mock 搬过来」的那份。**待删。**

**已做**:判据从「id 恰好叫 tone」放宽到「**这道题在问视觉方向**」——
`tone`(radio/checkbox)与模型开的 `direction-cards` 都由目录接管,前提是项目有视觉风格
上下文;没有上下文就原样渲染模型那几张。`direction-cards` 是单选。

**为什么换掉模型的选项不会「说两件事」**:答案是按 `formatFormAnswers` 拼成**文本行**
回给模型的(`- 视觉方向: Content-led product`),不是机器 id 契约;`tone` 那条路今天
就是这么替换的,已经在线上跑着。我一度把这个风险说重了,已更正。

红测:`tests/components/QuestionForm.direction-cards-catalog.test.tsx`(4 条,撤掉实现
复验会红 2 条)。

| 编号 | 现象 | 状态 |
| --- | --- | --- |
| B53 | (接 B39 照出)取消分页时「View all」按钮跟着退场,于是 `openGallery()` **失去全部调用点** —— 带分类页签(商务/编辑/创意/极简)的画廊弹窗在正常流程里打不开了,只有输入过自定义值、点 `.qf-visual-custom-summary` 才够得着;`visual_style_gallery_open` 埋点也基本不再触发 | `[x]` **弹窗整体退场**,不补入口。逐格核对交付稿 #21 / #22:底栏只有 `换一批 / 随机 / 下一步`,「铺开」是选项区右上角 `.vbar > .vswitch`(`aria-label="铺成网格"`)在 `fan` / `grid` 之间切、**是内联的**;全稿 84 格里唯一的 `role="dialog"` 是「联系支持」。那个弹窗本就是**分页时代的溢出面**(入口是卡片条末尾的 `+N`),分页撤掉后它没有存在理由;「看全部」由 `[data-action="toggle-view"]` 一次铺开整份目录承担。已清:`Dialog` 那一段、分类页签、`visual_style_gallery_open` / `visual_style_category_tab` 两个埋点、`interaction_source` 的 `'gallery'` 一档、`category_id` 参数、`.qf-visual-dialog*` 那族样式。停用的覆盖已开启并改钉收敛后的形态。**没有 i18n 键变孤儿**(`common.close` / `tool.done` / `qf.custom*` 都另有用处)。详见 §B53-1 |
| B54 | (同上照出)「随机」抽中的那张**没被翻到最前面**,选完还压在底下看不见。真因:`VisualStylePicker` 只传了 `revealToken`、**漏了 `revealValue`**,那个 effect 拿不到目标就 `at = -1`、弹回第一张。一沓只有 4 张时概率 1/4 撞对,整份目录进来才露馅 | `[x]` 已补 `revealValue`(旁边 `DirectionCardsPicker` 本来就传了,只有这一处漏) |
| B55 | 「为什么我发一句你好,然后下面都有这个产物卡片?按理说只有本轮增量的文件才会有呢」 | 打招呼那一轮**一个工具都没调**、daemon 结算的 `producedFiles` 是空数组,可底下照样压出一张整块的产物预览卡(带发布 / 导出) | `[x]` **真因不在卡片,在「本轮产出」这份名单**:`recoverLinkedProjectFilesFromContent` 会把正文里点到名的项目文件捞回来当本轮产出,判据是「落盘时间在本轮窗口内 **或** 那一行用了产出的口气」。真机那一轮(会话 `9a3a2f75`)agent 顺口复述了上一轮的成果 ——「我已经为你创建了一份 Kami 羊皮纸单页文档(`design-manifesto-parchment.html`)」—— 「创建」命中口气判据,于是**38 分钟前**写的文件被当成了本轮增量。B32 之前它渲染成一行长条,不显眼;改成产物卡之后就成了一整块。修法:口气那一条**只在这一轮真碰过文件时**才算数(`turnTouchedAnyFile`:产出清单非空,或本轮记到写/改工具调用);落盘时间那一条不受影响,真在本轮落的盘照旧认。红测先行(撤掉实现复验会红),并在同一份测里钉住反面:同一条消息只要把落盘时间挪进本轮窗口、或让 daemon 把它算进 `producedFiles`,卡必须照出 —— 「什么都不渲染」的回归过不了 |
| B56 | 「question-form 最底部那些按钮的样式要仔细对齐」—— 首步 `1 / 5` 同时出现「上一步 / 下一步」 | `上一步` 在首步仍被渲染,只靠 `disabled` 禁用;而底栏 ghost 按稿子去掉了禁用底色、保留同一档文字颜色,导致这颗**不可用按钮看起来仍可点击**。交付稿首步底栏明确只有 `跳过 · 你来判断 | spacer | 下一步`,全套稿件也搜不到首步「上一步」态 | `[x]` 首步不渲染 `上一步`;从第 2 步开始才出现。没有改分步交互。回归守卫逐一钉住底栏顺序:首步 `Skip / Next step`、后续 `Skip / Back / Next step`、视觉方向 `Shuffle / Random / Next`;主按钮 32px、跳过 loading/idle 同形和下一步门禁的既有像素/层叠测试继续全绿 |
| B57 | 「把这个『设计 ×』选项给下掉,首页那边也去掉;默认就是设计,不可选择」 | 首页和项目对话输入框都挂了 `ComposerModePicker`,虽然初值是 Design,用户仍能切到其它模式或清空选项 | `[x]` 两处生产入口都移除 picker;首页 `sessionMode` 固定为 `design`,创建请求继续显式携带 `conversationMode: 'design'`;项目对话保留既有 session mode 发送/程序化 next-step 语义,只撤掉手动选择入口。未改后端协议。测试钉住首页与项目对话都没有 trigger/clear,且首页提交值仍为 design |
| B58 | 视觉方向答完后只显示 `prototype-expressive-consumer` 这类内部 id,没有设计稿里的缩略图 | `direction-cards` 已由目录接管选择,但答案 normalization / summary 仍只认旧 `tone` 问题;当轮锁定态与历史回放还有两套映射 | `[x]` 统一目录判定和答案摘要 helper;当轮与回放都显示目录标题 + 选中预览图,不再泄露内部 id。普通 checkbox 原显示分组保持不变 |
| B59 | 已下掉的固定「继续处理 / 生成产物 / 更多」又出现在成功轮次下方 | `AssistantMessage` 只因本轮没有 `nextStepArtifactName` 就把普通 `default` 成功轮降成 `project-incomplete`,绕回旧固定菜单 | `[x]` 普通成功轮保持 `default`:有 agent `next_steps` 才显示,没有就不显示。Brand 专用恢复分支保留 |
| B60 | 错误卡的「联系支持 / 导出日志」退化成裸文字,可重试卡和不可重试卡还不是同一套按钮 | 卡壳已组件化,actions 仍散在 `ChatPane` / `ExportLogsAction`;共享 Button CSS Module 热更新脱载时会被 chat button reset 覆盖,另有裸 `<button>` 恢复路径 | `[x]` 新增唯一 `RunErrorCardAction`(底层仍是 shared Button),在卡片 CSS Module 固定 secondary / primary / sm;支持、导出、重试和普通恢复 CTA 全部接入。可重试/不可重试两态均有守卫 |
| B61 | 图片方向卡加载慢,翻到后面的卡片才开始出图 | 首屏虽然只有 6 张,但每张都是 1600×1200 原图,合计约 11.5MP;扇形隐藏卡统一 lazy,第 5/6 张可能等翻到才请求 | `[x]` 卡片用 Cloudflare 640×480 auto/quality75 缩图,活跃 6 张 eager + async decode;历史锁定态仍 lazy。样本约 54KB→10KB,解码量降至约 1.84MP |
| B62 | AMR 图片任务只报 `MEDIA_DISPATCH_FAILED / 原因未分类` | 本机旧 Vela 没有 `media` 子命令;Node 数字退出码 `1` 又被持久化成业务 code,agent 契约拒收后只能降级泛化错误 | `[x]` 本机 Vela 已独立升级到 `fee9d67fa`,test 目录含 `gpt-image-2`;OD 新增 `MEDIA_CLI_INCOMPATIBLE` 安全文案,且只持久化非空字符串业务码 |
| B63 | AMR `session/new` 等了 35 秒后显示「智能体版本不兼容」 | Go 的 `context deadline exceeded` 没进 timeout 词表,handshake 最后兜底误判成 `AGENT_CLI_SESSION_REFUSED` / update_cli | `[x]` timeout 分类补入该稳定短语;同一错误保持可重试 timeout,不再被 handshake guidance 改写成升级 CLI |

### B53-1 · 随弹窗一起没掉的「自己填」——**要产品拍**

那个弹窗里除了分类页签,还长着视觉方向题**唯一的自定义输入框**(`allowCustom` /
`customLabel` / `customPlaceholder`)。弹窗退场后这条路没了,现在按稿子实现 ——
**不自造一个稿子上没有的输入位**:

- 交付稿的视觉方向卡(#21 / #22)本来就没有「自己填」;稿子里的 `.opt.mod-own`
  只出现在 #19 / #20 那种**文字选项**上。
- 另一条视觉路径 `direction-cards`(`DirectionCardsPicker`)**从来就没有**自定义输入,
  所以「视觉题没有 Other 出口」在产品里已经是既成状态的一半。
- 目录外的答案仍然**看得见**:模型给的 `defaultValue` / 上一轮草稿会渲染成
  `.qf-visual-custom-summary` 那句陈述(B53 之前它是一颗按钮,点开弹窗;现在降成 `<span>`)。

**冲突点**:`apps/daemon/src/prompts/discovery.ts:81` 明写着「host 会在每道有限选项题
(含 `direction-cards`)上自动渲染一个 Other 出口」,并据此**要求模型不要自己写**
「其它 / 我来描述」这类兜底选项。视觉题这条路现在兑现不了这句承诺。

三条路,**没有原文依据,不自己拍**:
(a) 承认视觉题没有 Other,把 `discovery.ts` 那句话的适用范围从 `direction-cards` 摘掉;
(b) 给视觉题补一个稿子上没有的自定义位(两条路径一起补,别只补目录那条);
(c) 请设计在 #21 / #22 补一处放它。

## C. 长期约束(用户反复强调过的,不是单条 bug)

- **别留尾巴**:「要抽组件的就抽,要新实现的功能就实现,每个像素对齐过去」。
  推论:陈列页底下不许再写「⚠️ 这条要产品/设计设定」当挡箭牌 —— **交付稿就是规格**。
  确有产品口径要拍的(会改埋点样本口径 / 契约枚举),写进 `chat-panel-next.md` 的决策表并**同时给出按稿子的实现**,不要空着。
- **口头反馈当场落表**:见本文件抬头。
- **设计稿里的数据是模拟的**(2026-08-26 用户裁决):几张卡、多少条目这类**数量**不是规格。
  稿子那张视觉方向卡只画 4 张图,产品的目录有 26 张 —— 不能因为稿子是 4 张就不做「看全部」。
  设计同学不读代码,他们给的是**形态**;数量、分页、空态这些要按真实数据规模自己想清楚。
  推论:凡是「按稿子的数量照搬」的实现,都要回头检查一遍。

## E. 陈列页照不出真相的系统性原因(2026-08-26 查清,已修)

用户连续四次截图说「这个消息怎么还是这个样式的??」,而陈列页一直显示对齐 ——
不是某一格漏改,是**陈列页少内联了一整层样式**。

- **真相**:`apps/web/src/styles/viewer/routines.css` 里有一整块 `.app …` / `.chat-skin …`
  的旧聊天皮肤(**451 条**),`index.css` 把它排在**最后**(第 31 行;chat.css 第 13、
  composio 第 23、theater 第 24)。对同一个元素它**既赢特异性**(0,2,0 对 0,1,0)
  **又赢顺序** —— 产品里真正生效的是它。
- **陈列页原来一条都没内联**,所以「属性对齐」的绿章有一大半是假的。
- **已做**:
  1. 删掉把用户气泡刷成 `#ededed` 灰底圆角的那条(`.app .msg.user .user-text`),
     原位留注释说明这个陷阱;
  2. `buildPage()` 新增 `legacySkin`,按**这一页实际渲染出的类名**求交集只挑真会命中的
     几族,`<style>` 插在 `footerSkin`(theater)之后,照生产的层叠顺序。
- **重测**:属性差 360 → **128**(那 232 条是**假差异**,旧皮肤本来就盖成稿子的样子了)、
  位置差 728 → **773**、全干净 14 → **13/84**。
- **修法约束**:旧皮肤和稿子冲突时,**删掉 `routines.css` 里冲突的那一条**,
  不要在新组件上加特异性去压 —— 那是把同一场层叠战争再打一遍。
  删前确认这条不服务于 `.chat-skin` 那个宿主(`DesignSystemFlow` 的面板)。
- **位置差别逐条追**:773 条里 y=617 / x=470 绝大多数是**下游位移**(上面多一行,底下全跟着挪)。
  源头是高度 121 条 + **75 格结构对不齐**,改源头。

## F. 待办总表(2026-08-27 现场清点)

> 这一节只回答「**还剩什么没做**」。每条的**裁决**归各自权威文档,这里只给指针,
> 不复述决定内容 —— 复述必然漂移。

### F-1 报错卡(权威:`run-error-catalog.md` §6)

| # | 事 | 状态 | 关键事实 |
|---|---|---|---|
| E1 | **错误码映射表要补** | ✅ **已完成并合入** —— 补了 S19 六档(`process_crashed` / `signal_killed` / `terminated_unknown` / `exit_code` / `exit_nonzero` / `execution_failed`)、`account_suspended`(daemon 侧同时加了分类器)、`cpu_unsupported` 与 `AGENT_RUNTIME_DEF_INVALID` 改判第 4 档。红测 `run-error-ladder.test.ts` / `ChatPane.error-card-ladder.test.tsx` / `run-failure-account-suspended.test.ts`,逐块撤实现复验过 | 三张表**共 16 档**(`AGENT_AGNOSTIC_FAILURE_UI` 7 + `DETAIL_FAILURE_UI` 3 + `AGENT_AGNOSTIC_DETAIL_FAILURE_UI` 6)对 32 个场景。**`AGENT_EXECUTION_FAILED` / `process_exit` 两张表里都没有** → S19(每月 20,868 次、第二大类)整个落兜底 |
| E2 | **兜底不许把 `rawError` 摊在卡面** | ✅ **已完成,并在真机上看到了** —— 新增 `chat.runError.fallbackMessage`(19 语)。2026-08-27 真机:一次没有产物的运行,卡面是「这次没能顺利完成。原始报错收在下面的『查看详情』里…」,英文原文 `The design run finished without producing a deliverable project file.` 收进折叠区。**这正是修之前会直接摊在卡面上的那一串** | `ChatPane.tsx:1609` `displayError = messageKey ? t(...) : rawError` —— 没命中映射表时**直接显示上游原始错误串**。S19 今天「标题 + 一段 stderr」就是这么来的。补 E1 时要连兜底文案一起给 |
| E3 | **〔更换模型〕落点与稿子不符** | ⚠️ 待拍板,但**原来记的理由是错的** | 稿子 `error-ux-design.md:130`:「**直接打开模型选择器,选完自动重跑**」。实现落成 `onOpenSettings('execution')`,不自动重跑。<br>**更正**:原来写着「项目页里没有模型选择器,所以只能去设置」—— 不成立。`ProjectView.tsx:11324` 挂了 `AvatarMenu`,composer 那颗触发器点开就是内联的模型列表(`avatar-model-list` / `avatar-model-option`,2026-08-27 在真机上点开确认过)。设置里那个面板本来也是同一份选择器(见 `ChatPane.tsx:3086` 的注释)。<br>**所以真正待拍的只剩两件**:①落到内联那个列表还是设置面板;②**选完要不要自动重跑那一轮** —— 这一条是产品行为,前端不自己定 |
| E4 | 主按钮阶梯落到每一档 | ✅ **已完成** —— `primaryActionForFailure(nature)` 由 `RunFailureNature` 的三个问句(`directFix`/`transient`/`localDeadEnd`)推出按钮,全部调用点改走 `failureCard()`,仓里再没有手写 `primaryAction` 的字面量。**原则四从此是结构保证**:没声明 `directFix`/`transient` 的失败,函数根本产不出〔重试〕 | 阶梯见 §6.Z,对应关系见 §6.T。E1 补表时按阶梯算主按钮,**不要逐档再挑一次** |
| E5 | 四种身份的余额分支 | ✅ **已完成** —— 四组按 §6.V 落地(判据:`permissions.canManageBilling` × 新增的 `isMaxPlanTier`),死胡同(§6.Y)一并修掉:没有账单权限的成员改看 `AmrOwnerTopUpDialog`(可一键复制给所有者的话),聊天流水和首页两条路都换了。**真机只能验 owner/member 两轴** —— Max 档在 prod 造不出来,那一格目前只有单测 |
| E6 | 〔联系支持〕第 4 档提为主 | ✅ **已完成** —— 第 4 档把〔联系支持〕的 `variant` 提成实心主按钮,没有重排那一行(重排会动 e2e 的 274px 溢出判据)。**「提为主之后视觉上读不读得像主按钮」还需要人眼看一次** |

### F-2 todo 判据与召回

| # | 事 | 状态 | 关键事实 |
|---|---|---|---|
| T1 | **同一个判据有三份,口径不一** | ✅ **已完成** —— 三份合并成一个出处:`contracts/run-completeness.ts` 的 `isTodoWriteToolName` 改成大小写不敏感的正则(含 `write_todos` 与 MCP 前缀名),另两处改为 import 转发。红测 `packages/contracts/tests/todo-tool-name.test.ts`(6 条) |
| T2 | **AMR 把 `todowrite` 改成 `Todowrite`** | ✅ **已完成** —— 治本不靠下游宽容:`acp/updates.ts` 在**进 title 启发式之前**先认 todo 家族并归一成 `TodoWrite`。红测 `apps/daemon/tests/acp-todo-tool-name.test.ts`(4 条,喂 vela 真实帧形状:只有 kind 没有 name)。ACP 全套 80 条绿 |
| T3 | **召回通路不存在** | ✅ **已完成** —— `composeChatUserRequestForAgent`(唯一同时覆盖 `skipTranscript` 与非 skip 两条分支的钥匙孔)在每轮请求前**加一段陈述事实的短块**,说明这次对话还欠着什么;渲染器放 `packages/contracts` 免得文件系统档与 API 档漂移,措辞不点名任何工具(API 档没有工具)。没有未完成时组合出来的正文**逐字节不变**。判断继续/重排/忽略仍是 agent 的事,客户端不替它决定 | 历史是 `buildDaemonTranscript` 拼的纯文本,只取 `message.content`;而 `content` 只累加 `kind==='text'`(`db.ts:3060`),**TodoWrite 是 `tool_use`,永远进不了**。21 家无 resume 的 runtime **结构上不可能**知道上一轮有过清单 |
| T4 | 客户端 `previousTodos` 零调用方 | ✅ **已完成** —— `ChatPane` 按助手消息推出「这条消息之前对话已经声明过的清单」喂给 `buildTurnBlocks`。**只认 agent 自己重发的条目**:不重发就什么都不查、什么都不画 | `contract.ts:164` 定义了,没人传。**它是 T3 的下半段,单独接没用** —— agent 不重发,`previous.has()` 根本不会被调用 |
| T5 | **`isStruck` 的注释写反了**(代码是对的) | ✅ **已完成** —— 改的是**注释**不是代码。新表述:划线 =「这一条不是本轮新开的活」,三种情况独立(召回 / 作废 / 本轮开出来但没干过)。`ExecutionShell.tsx` 里那句复制的错注释一并改掉 |
| T6 | 测试洞 | ✅ **已完成** —— `build-turn-blocks.test.ts` 补上「召回 + 本轮继续做 → 既划线又可展开」,规格里最容易误判的那一格现在钉住了 |
| T7 | 〔继续未完成任务〕是**死按钮** | ✅ **已接上** —— 落在「还欠着活」那条消息的轮次状态行上(那一行本来就写着「已停止,还有未完成」)。本轮没声明清单时回落到带过来的那一份,所以**21 家根本不发清单的 runtime 也能用** | `ProjectView.tsx:8765` 实现完整、一路 prop-drill 到 `AssistantMessage.tsx:566`,**然后没有然后** —— 全仓 `onContinue=` 在 chat 组件里 0 命中。`origin/main` 上也一样死。**它是 agent 不照做时唯一的用户出口**,且不依赖任何 agent 能力 |

### F-3 「画出来了,没接线」(权威:`run-error-catalog.md` §6.U)

已接:`UpgradeCard` / `SupportDialog` / `support-brand-icons` / `PauseLine` ✅

| # | 事 | 状态 | 关键事实 |
|---|---|---|---|
| W1 | **`Reconnect` 仍未接线** | ✅ **已接线、已合入、已真机截图比对**(截图在 `docs/design/chat-mirror/shots-live-reconnect/`)。原作者纠正了盘点里一条**过期**的记载:重连的 `attempt/max/phase` 传输层**早就在发**(`providers/daemon.ts:291/307/327`),i18n 四个 key 19 语言包也齐 —— 「拿不到 N/M」不成立,是「铺好了没人接」。互斥做成了**结构保证**(暂停写在重连的 `else` 分支里),不靠两处判据碰巧不重叠 |
| W2 | `AudioArtifact` | 阻塞 | 卡在**数据层**:`artifactCardKind` 对 `.mp3` 返回 null,契约里没有波形与时长。**不是接线能解决的**,要产品+后端立项 |

### F-4b 新照出的(2026-08-27)

| # | 事 | 关键事实 |
|---|---|---|
| **R9** | **重连失败那一行和通用报错卡会同时出现** | ✅ **已修复并真机确认** —— `DAEMON_STREAM_DISCONNECTED` 落表且 `suppressCard: true`,整张卡不出;判据 `isReconnectOwnedFailure()` 和 `ProjectView.hasGenericDisconnectFailureEvent` 读同一对线索。2026-08-27 真机(杀掉 web dev server 造断线):流水尾部只有那一行「连接失败 +〔重新连接〕」,**上面没有任何报错卡**。`AGENT_CONNECTION_DROPPED`(S11)刻意没碰 —— 那是 agent 到模型服务的连接,重连行接不回来 |
| O6 | 重连循环**无退避** | ✅ **已修复并真机确认** —— 五次尝试之间零 `setTimeout`,而连接被拒的 fetch 约 1ms 就 reject,所以整个预算在同一个 tick 里烧光:①合盖子/切 Wi-Fi 这种几秒自愈的抖动被判成不可恢复;②那一行**没人看得见**。现在复用 `lib/backoff.ts` 的 `BackoffController`(700ms 起、×2、封顶 8s、带抖动),这条流原本是仓里唯一漏掉退避的。红测 `daemon-sse-reconnect-backoff.test.ts` 两条都先红过。**真机实测的相邻间隔:0.4s / 1.3s / 2.1s** —— 修之前这三档全在同一帧里 |
| **R10** | **按〔重新连接〕会把那一行撤掉,而重连根本没起来** | ✅ **已修复并真机确认**(真机照出来的,单测照不出)。`handleManualReconnect` 乐观地推了一条 `dropped` —— 可 `dropped` 的语义是「本地不再跟这条流了」(切会话、卸载),按重连恰恰相反。重挂有前置条件(先要拉到这一轮的运行状态),**而断线时那一条也常常拉不到**,于是重挂没开始、那一行却已经没了:屏幕上只剩壳头一句「运行失败」,报错卡又被 R9 按设计压掉,**用户连再点一次的入口都没有**。改成新的 `manual-retry` 信号(什么都不做);撤那一行的唯一时机仍是 `reattachDaemonRun` 前一行那条 `dropped` |
| **R11** | **结局从「别的门」进来时,重连行留成残影** | ✅ **已修复并真机确认**(是 R10 修完才露出来的第二层)。`settled` 今天只在**流上**发,可这一行出现的时刻正是流断了的时刻 —— 结局往往由一次会话刷新带回来。真机上看到那一行挂在一条写着「已完成」的消息下面继续说「连接失败」。补了 `settledSignalFromMessages()`;撤不撤仍交给 `nextChatReconnectView` 判(`failed` 对已交回给人的那一行**不动**,否则 22-3 那颗按钮会一闪而过) |
| **R12** | **被 `kill -9` 的运行被判成「登录过期」** | ✅ **已修复并真机确认**(真机照出来的第二个)。真跑一次 `kill -9`:run 落库 `signal: SIGKILL / exitCode: null`,分类器却给 `auth / stale_profile` —— 因为缓冲区里碰巧留着半句 "stale local profile",而**纯文本匹配排在信号判定前面 300 行**。卡于是让用户去跑 `/login`,修一个没坏的东西。S19 是第二大失败类(20,868/月、16.3%),等于在这个量级上贴错标签。改成**强制信号(SIGKILL + 崩溃信号)先认**;SIGTERM/SIGINT 不动(那两个确实伴随优雅关闭与中断,文本更有信息量),超时也仍然优先(看门狗会先写自己的理由再升级到 kill)。修完同一次 kill 给出 `process_exit / signal_killed`,卡面是「智能体意外退出」+〔重试〕,英文原文收进「查看详情」 |
| O8 | 读数**永远走不到 5/5** | 真机 trace 是 `1/5 → 2/5 → 3/5 → 4/5 →(判失败)`。原因是结构性的:读数记的是「已经用掉几次」,第 5 次用掉的那一刻循环条件就不成立了,于是 5/5 只存在几微秒。**要让它可读只能让它停一拍**,而停一拍时其实并没有在重试 —— 是「读数记已用次数还是记在飞的那一次」的口径问题。稿子把第 83 格明写成「**不是独立形态**」,所以这不算违反交付稿。**留给产品定,不自己改口径** |
| O7 | 重连行的 ⌄ 展开箭头**故意不给** | 传输层今天把三种断法(接口/超时/服务端)走同一条路径,分不出断因。摆一颗点开什么都没有的箭头比不摆更糟 |

### F-4 其他已知、暂不处置

| # | 事 | 关键事实 |
|---|---|---|
| O1 | `resumable` 硬写 `false` 两处 | **查清了:两处都不是「随手写死」,是缺输入 / 刻意 fail-closed,所以不能当接线漏改去补。** ①`runtimes/runs.ts:741`(daemon 重启后的对账):`resumableFailure` 需要 `liveSessionId` + 实时观察到的 `committedWorkSeen`,而这条路是**从落盘状态重建**的 —— `state.json` 里**根本没有 sessionId 字段**(全文件零命中)。要让它算得出来,得先把 sessionId 与「已产生副作用」落盘,那是一件独立的事。②`ProjectView.tsx:12572`(跑完了没产物):这是**客户端合成**的 `ARTIFACT_NOT_FOUND`,daemon 侧那一轮是 `succeeded`,压根没算过 resumable;这里写 false 是不说谎。真正的缺口在上游 —— 「跑完但没交付物」该不该给〔继续运行〕是产品裁决(§6 对 ARTIFACT_NOT_FOUND 的 `report_only` 口径),**不是前端能自己定的** |
| O2 | 团队成员余额死胡同 | 见 §6.Y。没有账单权限的成员余额耗尽时弹窗**只有一颗「暂不需要」** |
| O3 | B11 未真机验 | ✅ **真机端到端验过了**(2026-08-27)。三条硬证据:①端点返回 `{"ok":true,"delivered":true}` 且返回的 run 仍是同一个;②那个 runId 在会话里**只出现 1 次** —— 没有为这条消息新建 run(这是区分「引导」与「打断重发」的判据,不是「界面有没有变」);③**模型在同一轮里回了「收到,行数表…」并照做** —— 这正是原作者明说「我证明不了」的那一环(它只能用假 claude 证明帧到达 stdin)。真实 `claude --input-format stream-json` 确认会消费第二个 user frame |
| O4 | e2e `chat-error-card-layout.test.ts` | ✅ **它关心的那件事已经在真 Chrome 上量过了**(文件本身仍待 CI 跑,本机不装 playwright)。用 `Emulation.setDeviceMetricsOverride` 逐档收窄真页面上那张 S19 卡:420px 视口 → 卡 368px,三颗动作**同一行**(89+89+56),不溢出;310px 视口 → 卡 **258px**(已低于 274px 判据),两颗次级留在一行、主按钮〔重试〕**换行且仍右对齐**,`overflowsCard: false`、页面无横向滚动、按钮文字**零裁切**。也就是说窄下来是**优雅换行**,不是溢出。截图 `shots-live-errors/s19-narrow-258px.png`。<br>另外把那个文件**逐条读过一遍**,确认它不会「不报错地什么都不守」:①它依赖的两个钩子 `chat-run-error-card` 与 `data-user-action-footer` 在现在的 `RunErrorCard` 里都在,而且 `ChatPane` 唯一那处调用把 `dataKind="run-recovery"` 写死了,所以 footer 属性**恒在**(它是条件属性,`dataKind` 一旦为空整排按钮就数不到);②断言口径和我实测的一致 —— 不许横向溢出、按钮都在卡内、**允许换行**(它自己的注释就写着「动作行会换行,下标不再等于那一对」,所以按具体按钮比 y 而不按下标)。**本机跑不了是硬限制**:配置用的是 playwright 自带 Chromium(没有 `channel: 'chrome'`),本机没装,而装浏览器是禁止项 |
| O5 | B41 compare 截图 | ✅ **重拍并逐格重跑过了(2026-08-27)**,结论是**一个数都没动**:`diff-cells.mjs 1 84` 的产出和存档的 `diff.json` 完全一致(属性差 103、位置差 611、未配对 1036、全干净 2)。这一轮的改动是行为层的,没有移动任何像素 —— 这本身就是要的答案。**顺带把这把尺子的两处噪音标定清楚了,见下面 O9 / O10**,别再拿它们当缺陷追 |
| **O9** | **含球的格子,位置差全是假的** | `Orb` 的画布尺寸是在 `useEffect` 里写的(`canvas.style.width = box + 'px'`),而陈列页是 `renderToStaticMarkup` —— **不跑 JS**。于是稿子那边球是静态 24px,我们这边量出来 20px,summary 高度跟着 36→32,整格往下的每个元素都错 4–5px。第 1 格 73 条位置差绝大多数是这一条派生的。产品里是对的(真机 DOM 就是 `width:24px;height:24px`)。**含球的格:1 / 9 / 12 / 13 / 71 / 82 / 83** |
| **O10** | **「未配对 1036」严重虚高** | 配对靠**文字内容**,于是两类合法差异会让元素永远配不上:①夹具耗时不同(稿子 `18.2s` vs 我们 `0.7s`);②增删行数用的减号不同 —— 稿子是 ASCII `-`,我们是 U+2212 `−`。**后者已量过:在 `.delta` 用的 mono 里 `+` / `-` / `−` 都是 7.227px,整串 `+182−58` 与 `+182-58` 同为 50.578px —— 零像素差,不用改。** 这一栏只能当线索,不能当账 |

### F-2b S12「等太久没动静」

| # | 事 | 状态 |
|---|---|---|
| S12 | 等待期间**一句回音都没有** | ✅ **已实现并真机确认** —— 壳带上 `quietMs`(上一件事之后过了多久,纯函数层从已有的秒级 tick 推,组件不自己起计时器);超过 60 秒壳头换成稿子逐字的「上游响应慢，已等 N 秒」,一有东西落下来自动退回。**静默起点是传输层如实登记的「最近一帧什么时候到的」**(`runtime/chat/upstream-activity.ts`)—— 不是事件条数,也不是事件自带的时刻,理由与真机复现见 F-8 的 ②b。`startedAtMs` 专为「卡在首个 token」那一格服务(每月 5,547 次,事件流里一个带时刻的事都没有)。**〔停止〕那一半不新加控件** —— 输入框那颗停止键一直都在。**超时判定一个字没动。** 红测两份,并撤掉实现复验过。真机:第 61 秒准时出现 |

### F-8 用户真机连点名的一批(2026-08-27 下午)

> 这一批全部是用户在自己 Chrome 里边用边指出来的,不是我扫出来的。
> **已修的**都红测先行 + 真机复验;**没修的**在下面 F-9,别当成已完成。

| # | 用户原话 / 现象 | 根因 | 状态 |
|---|---|---|---|
| U1 | 「音频产物外面不要套大卡片了啊,只有一个音频的横的这个就行了呀」 | 我把音频塞进了缩略图卡壳:252px 空方框 + 卡壳自带的〔导出〕浮层压住右端时长 | ✅ 音频不再是一种 `artifactCardKind`,单独走 `audioEntries` 画裸胶囊(42px),并同时排除出文本行,免得一个文件出现两次 |
| U2 | 「这个总是有事没事就出现…只有在很上面时才出现不行吗」(回到最新) | 判据是写死的 `distance > 120`,**同一个门槛在 ChatPane 里写了四遍** | ✅ 抽成 `runtime/chat/jump-to-latest.ts`:门槛按视口高度算(0.75,下限 320 上限 1200),出/收两档迟滞防闪。真机实测 200px 不出、900px 才出 |
| U3 | 「hover 不是应该变成稍微亮一点点的颜色吗…而不是一整个按钮全变成白色」 | **特异性**,不是哪个组件写错:裸的 `button:hover:not(:disabled)` 是 (0,2,1),压过组件自己的单类 hover (0,2,0) | ✅ 两份都包进 `:where()` 归零。⚠️**那条规则有两份拷贝**(`apps/web/src/styles/primitives.css` 和 `packages/components/src/styles.css`),我先只改了前者,在真机上量出来两条规则都还在、还是白的 —— 测试现在两份一起钉 |
| U4 | 「之前不是说如果 done 之前,没有任何工具调用或 thinking 或普通文案,就不出现这个了吗?」 | B47 把 `failed` 和 `canceled` 一起留了空壳。可那条理由(壳头是唯一说得出「出事了」的地方)**只对 failed 成立** —— 取消那档壳头写的是「进行中」,压在底下那行「已取消」上面 | ✅ 空壳只在 `failed` 时保留 |
| U5 | 「为什么还会有这种错误样式?? 你的错误卡片呢??」「设计稿里哪有这种状态行」 | `.status-pill.is-error` 只在「这条消息**正好拥有**报错卡」时才藏,于是**任何历史失败轮次**都把上游英文原文顶着红框戳在对话里 | ✅ `error` 一律不出。`warning` / `initializing` 早就按同一道理去掉了,这是漏网的一档 |

**U5 顺带回答了他追问的那句「难道还有错误没有梳理并包成错误卡片的????」**:
有 —— `retryableAssistantMessage` 只认**最后一条**消息,所以报错卡只给最后一轮
(和「回合状态行只在最后一轮出」是同一条产品裁决)。历史失败轮次现在由壳头那句
「运行失败」承担,上游原文归卡上的「查看详情」,不再裸奔。

### F-9 ⚠️ 用户已提出、**尚未修**的(接手请从这里开始)

> N4 已经做完了,留在表里是为了把「查到哪儿、推翻了什么」写清楚。

| # | 用户原话 / 现象 | 已查到的根因 | 下一步 |
|---|---|---|---|
| **N1** | 「为什么我们已完成旁边没显示耗时??」 | **根因已定位**:`shellElapsed()` 只从**事件上的时刻**推跨度(`shellSpan` / `firstStartedAt` / `lastEndedAt`),而很多 agent 根本不给事件打时刻 —— 真机上一条 succeeded 的 run 26 个事件里**只有 9 个带时刻**。可 `message` 自己是有 `startedAt` **和** `endedAt` 的,那才是最权威的跨度,却没人用 | 给 `BuildTurnInput` 补 `endedAtMs`(`startedAtMs` 我做 S12 时已经加了),让 `shellElapsed` 在事件推不出跨度时回落到「消息自己的起止」。注意 `formatShellElapsed` 有 **1000ms 地板**,低于 1 秒一律不显示 —— 我那条 100ms 的夹具就是这么消失的,别被它误导 |
| **N2** | 「thinking 也要包裹在进行中的下拉卡片里,其次 thinking 完成后就变成普通工具调用的状态,可以下拉展开看思考细节」 | 现在 thinking 落成 `ShellText{thinking:true}`,由 `SayText` 画成**裸段落**,一轮结束就把全文摊开。**设计稿是支持他这个说法的**:`chat-panel-next.html` 的组件 3 就叫「Thinking」,原话「Thinking 那块用的是**同一副壳**、里面也是 `.think`」,而且「Thinking」和「进行中 / 已完成 / 运行失败」并列被称作**分段头** | 把 thinking 段渲染成 `Foldable`(头= Thinking,默认收起,展开看推理),**流式中那一条除外** —— 稿子里「进行中的一行」是另一样东西(`.thinking-live`),它「不套框、不挂耗时」,只活到第一个字落地 |
| **N3** | 「你这左右/下面的间距呢? 去看看设计稿」(截图是 question-form 的输入框,红框标着左右和下方几乎没有留白) | 未查 | 拿 `<question-form>` 那一格去比设计稿的内边距。**记住教训**:必须在浏览器里量计算样式,只 diff CSS 文本会漏掉层叠反转 |
| **N4** | 「这又是啥啊,不应该是一个普通工具调用的样式吗? 创建 xxx?」(截图是 `写入 design-manifesto-parchment.html` 底下摊着一大块 HTML 源码预览) | ✅ **已修**。**不在 `ToolRow` / `build-turn-blocks`** —— 那两处一直是对的(Write 跑完就是「新建 <文件名> +N −0」一行)。那块源码是 `AssistantMessage` 在**壳外**另起的一张流式代码卡 `LiveCodeBox`:daemon 的 `tool_input_delta`(`claude-stream.ts:619` 转 claude 的 `input_json_delta`)→ `ProjectView` 按 tool id 累成字符串 → `ChatPane` 穿透 → 落成 `live-tool` 块。所以它**只在流式期间存在**,`tool_use` 一落库就换回那一行,刷新页面也不会再出现 —— 但一份大 HTML 要流几十秒,用户看到的就是那几十秒。头上那个「写入」用的还是老键 `tool.write`(新链路的动词是 `chat.record.verb.write` =「新建」),这也是截图里的字对不上新壳的原因 | **修法**:整条 `liveToolInput` 链路删掉(ProjectView 累料 → ChatPane 穿透 → AssistantMessage 落块 → `LiveCodeBox`)。依据是 D3(`chat-panel-next.md:413`)与 B8(`:754`):调用没配对结果就不出行。这**推翻了接入时的临时安排**(`chat-panel-next.md:674` 第 1 条「位置不对但能力不丢」)—— 「不丢」被 N4 明确否掉。顺带省掉一处按 token 打的 `setState`(每个 `input_json_delta` 都在 setLiveToolInput,大文件是几千次 ProjectView 重渲染)。**没动**的是 `<artifact type="text/html">` 那条流式代码卡:它防的是半截 HTML 当 markdown 正文漏出来,不是工具调用。红测 `apps/web/tests/components/chat/write-live-preview.test.tsx`(4 条,含终端输出的反面守卫) |

### F-10 claude 为什么写不出 todo(查清了)

**不是我们限制的。** `apps/daemon/src/runtimes/defs/claude.ts` 给的参数里没有
`--allowedTools` / `--disallowedTools`,`~/.claude/settings.json` 的 `deny` 也是空的。

直接跑一次 daemon 用的同一条命令、抓它自己的 `system/init` 帧:

```bash
printf '%s' '{"type":"user","message":{"role":"user","content":[{"type":"text","text":"hi"}]}}' \
  | claude -p --input-format stream-json --output-format stream-json --verbose \
  | python3 -c "import sys,json;[print(len(d.get('tools',[])), 'TodoWrite' in d.get('tools',[])) for d in (json.loads(l) for l in sys.stdin) if d.get('subtype')=='init']"
```

结果:**它声明了 68 个工具,里面没有 TodoWrite。** 这台机器上装的 claude 构建
(工具表里有 `Workflow` / `CronCreate` / `EnterWorktree` / `ScheduleWakeup` 这些)
就是不带它。所以落盘的 18 条真实 `TodoWrite` 事件**全部来自 codex**。

⚠️ **我先前判断错过一次**:看到有 18 条就断定 claude 在瞎说 —— 那 18 条不是它的。
教训已存记忆(`feedback_dont_trust_agent_self_reported_tools` 需要更正:自述不可信
成立,但**反证也要看清是谁的证据**)。

**顺带一个 daemon 侧的缺口**:`claude-stream.ts:380` 处理 `system/init` 时只留了
`model` 和 `session_id`,**把 `tools` 数组丢掉了**。所以事后完全无法从落盘数据回答
「那一轮 agent 到底有哪些工具」。要查只能像上面那样重新跑一次。

### F-7 真机验到哪一步(2026-08-27)

| 面 | 结果 |
|---|---|
| S29 重连三态 | ✅ 82/83 计数、84 失败 + CTA,三次不同断法都跑过,截图在 `shots-live-reconnect/` |
| R9 断线不出报错卡 | ✅ 两次独立断法都确认流水里只有那一行 |
| R10 / R11 CTA 死胡同 | ✅ 修前修后都在浏览器里走过 |
| E2 兜底文案 | ✅ 一次「没有产物」的运行照出来的 |
| S12 等太久 | ✅ 第 61 秒准时出现,截图在 `shots-live-s12/` |
| S19 进程被杀 | ✅ 真 `kill -9`,顺带照出 R12,截图在 `shots-live-errors/` |
| 报错卡窄面板 | ✅ 逐档收窄量到 258px,见 O4 |
| **组件 20 · PauseLine** | ✅ **看到了**。它要「手动停止 + 本轮还欠着 todo」两个条件同时成立,而本机 claude 这几轮一条 TodoWrite 都没发(落盘的真实 tool_use 全来自 codex)。走执行设置把 agent 切成 Codex CLI —— 那份配置在 **worktree 本地的 `.od/app-config.json`**,和用户自己的环境隔离 —— 跑一个五步计划,等 TodoWrite 落下来再按停止。结果:`chat-pause-line` 在场、写着「已手动暂停任务」,重连行不在场(互斥成立)。截图 `shots-live-pause/` |
| **R13** | **〔继续剩余任务〕的字整个漏在按钮外面** | ✅ **已修复**(PauseLine 一渲染出来就看见了)。那一行读作「已取消　继续剩[余任务]」—— 六个字压在旁边的状态词上。T7 接线时挂了 `.assistant-copy-button`,那是**图标**用的固定 26×26 方格(`padding: 0` + `overflow: visible`),文字在里面既不换行也不裁切,直接画到盒子外面:压住邻居,而且只有前 26px 点得到。改成让它按内容撑开、自带左右内边距,悬停/焦点仍沿用同一套;另加一条反向断言钉住图标按钮**不许**跟着放开。修完实测 94px(原 26px),内容装得下,与任何兄弟元素零重叠 |

### F-6 套件状态(2026-08-27 收尾实测)

| 面 | 结果 |
|---|---|
| `pnpm typecheck` | 0 错(含 e2e 那套更严的) |
| `pnpm guard` | exit 0 |
| `@open-design/web` 全量 | **700 / 700 文件、7287 条全绿**。开工时是 **14 条红的** —— 逐条把每个 chat 源文件 checkout 回合并前的提交复跑过,**同样红 14 条**,所以全是先前就欠下的,不是今天弄坏的。它们钉的都是**后来被用户推翻的形态**(B47 空壳、回合状态行只在最后一轮、D43 正文归属、分界线落在新会话、TodoCard 已删),逐条按现行裁决改了断言并写清依据 |
| `@open-design/daemon` 全量 | 8743 / 8750。3 条是并行压力下的抖动(`codex-session-resume` / `media/tasks-routes` 单跑即绿),1 条是**分支落后 main 104 个提交**导致的:`chat-project-skill-critique-label` 打的是 odNext strategy recipe 那套 main 上有、分支上还没有的代码;分支内改它等于修一份 104 个提交前的快照,**留给 rebase/merge main 时自然消解** |
| 真机(用户自己的 Chrome + 真 daemon) | S29 三态、R9、R10、R11、E2 兜底、S12 全部现场跑过并截图 |

### F-5 这一轮学到的几条(避免重犯)

1. **陈列页照不出真相有两个原因,不是一个**:①少内联旧皮肤(已修);②**陈列页直接 import 组件,而产品根本不渲染它** —— 后者陈列页结构上照不出,6 个组件中招。
2. **先查 `specs/current/` 再查代码**。`chat-panel-edge-audit.md` 早把 `cancelOrigin` 那条(R8)写清楚了,我却直接去 grep 代码,绕一圈还漏了两个 daemon 写入点。这个目录下有 11 份 `chat-panel-*`。
3. **验真机之前先确认跑着的进程是不是新代码**。我拿一个合并前就起着的 daemon 验「引导对话」,
   端点根本不存在。`tsx` 不热更 daemon —— **合并完必须重启 daemon**,web 才是热更的。
4. **看状态码不看 body 会把自己骗过去**。同样是 404:Express 的「没匹配到路由」返回 **HTML**
   (`Cannot POST /api/...`),而业务的「run 不存在」返回 **JSON**(`{"error":{"code":"NOT_FOUND"}}`)。
   我第一次只看到 `404` 就断定「路由在」,方向整个反了。**判据要落在能区分两种情况的那个字节上。**
5. **UI 上「点了有反应」不等于走了你以为的那条路**。第一次 steer 测试里队列确实清空、消息确实出现,
   但那是**队列按普通发送流走了**(数据里多出一个新 run)。分辨的判据是「**有没有新建 run**」,
   不是「界面有没有变」。
6. **量任何布局之前,先确认那个 CSS Module 的规则真的在页面里。** 我两次差点把「样式没加载」
   报成「实现有 bug」:①杀掉 web dev server 之后第一次挂载 `Reconnect`,它的 CSS chunk 取不回来,
   截图里 wifi 图标有几百像素高;②HMR 循环多轮之后 `record.module.css` 从 `document.styleSheets`
   里**整个消失了**(`record_` 规则数从 117 掉到 0),于是 `summary` 退回 `display: list-item`,
   球和文字堆成两行。判据很便宜:遍历 `document.styleSheets` 数一下那个前缀的规则条数,
   0 就说明这次测量没有意义 —— 重新加载页面再量。
7. **杀掉 web dev server 会让页面在服务重启后自动 reload**,React 状态全丢。要观察断线态的后续,
   得在服务还没回来之前把该看的都看完;想留住状态就别用这个手法,改成在页面里替换 `fetch`。
8. **agent 自报的能力不可信。** 追 PauseLine 时 claude 明说「这个会话里没有 TodoWrite 工具,
   检索过也确认没有」,我差点当成产品缺陷记下来。翻落盘的 run 日志:**18 条真实的
   `TodoWrite` tool_use 事件**躺在那儿。它就是在瞎说。**问工具有没有,去数据里数,别问模型。**
9. **单测绿了不等于修的是同一条路。** R12 我第一版写的红测用 `errorCode: 'AGENT_SIGNAL_SIGKILL'`,
   两条红→绿、很好看;可产品**根本产不出那个形状** —— 真机 `kill -9` 落的是
   `AGENT_EXECUTION_FAILED`,信号只在 `status.signal` 上。于是"修复"在单测里成立、在浏览器里
   一个字没变。**造夹具之前先去看一眼真实那条记录长什么样**;这条已经把真机形状钉进测试文件了。

## F-11 两条待用户拍板的(2026-08-27,已查清真因,未动手)

### N2 thinking 折叠 —— **用户要求与设计稿相反**

**设计稿(组件 3「Thinking」)写了三态**,原文逐字:

| 状态 | 原文 |
|---|---|
| 进行中 | 「一扇**定高**的推理流,可收起,不套框」(`--stream-h: 96px`,上下 32px 渐隐,内容自己往上走) |
| 内容开始落地 | 「这一行**让位给开场白,不再占地方**」 |
| 跑完 | 「收进 7·任务进度里,是几段纯文字,**不再自带折叠**」 |

**用户要求**(2026-08-27):「thinking 完成后就变成普通工具调用的状态,可以下拉展开看思考细节」
—— 即**新增一层折叠**,与「不再自带折叠」直接冲突。

**真因已查清**(两边都同意现状是 bug,只是解法不同):
`ExecutionShell.tsx:52` `streaming = running && shell.thinking`。thinking 期间 body 挂
`.stream`(限高 96px);而 `build-turn-blocks.ts:483` 在**第一段正文或第一个工具落下时**
立刻 `shell.thinking = false`,body 从 `.stream` 换成 `.stack`(高度 auto)——
刚才那十几段推理**原地全部展开**。这就是用户说的「一结束全部释放出来了」。

- **A(设计稿)**:第一个字落地时 thinking 那几段从流里撤走,不再占位;跑完后它在执行记录内,
  而记录跑完自动收起(`lifecycleOpen = running || stopped`)。**不新增折叠层**,不制造壳中壳。
- **B(用户原话)**:跑完后包成「Thinking」折叠行,默认收起。

**状态:等用户拍板。** 若选 B,必须在此标注「用户裁决覆盖设计稿组件 3 状态 3」,防止后人改回。

### N5 夹心正文不缩进、不接线 —— 设计稿规则被真实数据打穿

**用户提问**:「如果工具调用上面就是正文,是不是不用这个缩进?」

**结论与用户猜测相反**:工具行缩进 22px 是设计稿要的,`chat-panel-next.html:1457` 与我们
`record.module.css:194` **逐字相同** —— `padding: 5px 7px 5px 29px` 配 `margin-inline: -7px`,
`29-7=22`,让每条步骤的名字落在状态点右边同一条竖线上。正文默认在 0,**开场白就该贴左**
(设计稿:「开头那句…不动:它是整块的开场白,不在链上」)。

**真正缺的是另一半。** 把真实录制(`tests/fixtures/chat/claude-shop.turn0.json`)喂进
`AssistantMessage` 渲染,壳内孩子的实际序列是:

```
p.think │ div.tool ×4 │ details.fold ×2 │ div.tool │ details.fold ×5 │ div.tool │ … │ p.think ×6
```

工具行有**两种 DOM**:能展开的(命令、有输出)走 `Foldable` → `details.fold`;
不能展开的(读取 / 改写 / 搜索)是 `ToolRow` 直接返回的 `div.tool`(`ToolRow.tsx:40` `rowClass`)。
而设计稿那条「夹在两步中间的正文右移 22px + 接竖线」的规则是:

```css
.fold ~ .think:has(~ .fold)      /* 设计稿 1432;我们 record.module.css:171 —— 逐字照搬 */
```

**只认 `.fold`。** 用户截图里那几段正文前后全是 `div.tool`(改写文件),一条都匹配不上,
于是既不右移也没有竖线,全部贴最左 —— 工具行孤零零缩进,看着像「工具行莫名往右挪了一格」。
连接线规则(设计稿 1409-1410)同理,也只画在 `.fold` 上。

设计稿 demo 里步骤大多可展开,所以照不出来;真实产品里「读取 / 改写」这类**没输出的调用占多数**,
正文夹在它们中间是常态。设计稿注释写的意图是「**前后都还有步骤**,中间这段才该接上」——
说的是「步骤」,不是「可折叠的步骤」。

**拟修**(等用户点头,视觉会变):两条选择器放宽成认两种步骤形态 ——
`:is(.fold, .tool) ~ .think:has(~ :is(.fold, .tool))`,连接线同理。

**注意**:`Foldable.tsx` 的文件注释早写过这个坑 ——「`expandable === false` 时……仍然用
`<details>` 而不是换成 div —— **结构一变,父层那些按嵌套层数算缩进的选择器就全部错位**」。
`Foldable` 自己守住了,但 `ToolRow` 的简单形态压根没走 `Foldable`。

## F-12 真机验收挖出来的四条(2026-08-27,已修/已定性)

### ① claude 的推理被空 delta 吞掉(daemon,已修)

用户:「关键它 thinking 界面没有任何反应或反馈啊」。

Claude Code 发的 `thinking_delta` 帧里 `thinking` 是**空串**;**32 个录制里 1707 条
thinking 事件、1508 条空的(88%)**,真正的推理只在消息结尾那个 `assistant` 帧的
`block.thinking` 里。而 `claude-stream.ts:731` 收到 delta 就**无条件**把该消息记成
「推理已经流过了」,于是消息结尾的兜底分支(`:557` 的 `!thinkingAlreadyStreamed`)
被跳过 —— 一个字都没送出去。

后果是设计稿组件 3 那扇 96px 的推理窗口对 claude **一直是空的**:壳头写「思考中」、
底下什么都没有;过了 60 秒连「思考中」都被 S12 换成「上游响应慢」。

修法:只有**真的带了字**的 delta 才算「流过了」。红测在
`apps/daemon/tests/runtimes/claude-stream-thinking.test.ts`,另配两条守卫防止修成
「流式和尾部各发一遍」。

**只有 claude 中招。** 扫全部录制:claude 32 run / 1707 thinking / 1508 空;
codex 11 run / **0 条 thinking**(它的推理走 `reasoning` item 另一条路)。
两家的 `text_delta` 都是 0 空串 —— 坏的只有 thinking 这一条通道。

### ② S12 的静默计时只认带时刻的事件(web,已修)

用户:「这个是真的上游响应慢吗 还是我们的什么解析 bug 啊?」

**判据是两个数相等**:截图上「已等 86 秒」和总耗时 `1m 26s` 是同一个数 ——
说明整轮没有任何一条事件被打上时刻,静默起点只能退回轮次开头。

真机 run `ab4779f8`:**119 条 agent 事件,只有 12 条带时刻**,其余 107 条是
thinking / tool_input 增量。模型一路吐字,界面报「上游响应慢」。

修法:`BuildTurnInput` 新增 `lastEventAtMs` —— 客户端自己观察到的**到达时刻**,
是传输事实,不依赖任何 agent 填字段;带时刻的事件仍优先(更准),它只在更晚时接手。

**这条修的是全家**:规格点名 qwen / deepseek / grok-build / aider / antigravity /
atomcode / qoder 都不发工具事件,原来它们全会误报。

#### ②b 第一版取到达时刻的方式是错的(web,已修)

到达时刻这个判据没问题,**取它的钥匙**错了:`AssistantMessage` 当时写的是
`useMemo(() => Date.now(), [displayEvents.length])` —— 事件条数一变就重取。
可那个数在整段流式期间根本不动。真机 run `7ed15c2f-8ea0-4e55-b7e3-e463037dd868`
(1357 行落盘)壳头写着「上游响应慢，已等 156 秒」的那一刻,事件流里带时刻的事
确实停了 161.6 秒(`tool_result` +676.1s → `tool_use` +837.7s),而**这 161.6 秒里
落了 126 条帧**,平均 0.7 秒一条。三条原因叠在一起:

- `tool_input_delta` 在 `providers/daemon.ts` 就被岔进 `onToolInputDelta`,
  **不变成 `AgentEvent`**,不进 `message.events`。它是这条 run 里 1346 条 agent 帧
  中的 699 条,更是那个窗口里 126 条中的 124 条;
- claude 的 `thinking_delta` 一律空串(这条 run **414/414** 条 `delta: ""`),
  `appendBufferedAgentDeltas` 的 `if (thinkingDelta)` 把空串挡掉,事件数组
  **连引用都不换**,连 `useMemo(…, [events])` 都不会重算;
- 就算带了字,连续的 `text` / `thinking` 会被 `appendCoalescedAgentEvent`
  合进**最后一条**,长度同样不涨 —— 所以这条对 plain-stream 那批也不成立。

同一份 run 里这种窗口有四个:305.2s / 87.6s / 161.6s / 70.9s,而四个窗口里的
真实最大静默只有 2.3s / 2.2s / **73.6s** / 2.1s ——「上游真的慢」在这一轮里只发生过
一次,却报了四次。

修法:静默起点改由**传输层**如实登记 —— `runtime/chat/upstream-activity.ts` 按 run
记「最近一帧什么时候到的」,`providers/daemon.ts` 在 `sawRunEvent` 那一支叫它
(keepalive 注释帧**不叫**:那是我们自己的心跳,证不出上游在干活),
`AssistantMessage` 在既有的每秒 tick 上与 `nowMs` **同刻**取一次。
登记不到就不传,让 `shellQuiet` 退回轮次开头 ——「卡在首个 token」那一档要的正是这个。

红测 `apps/web/tests/components/chat/s12-upstream-alive.test.tsx`:真实传输层 +
真实 `AssistantMessage`,帧形状逐字抄自上面那份落盘。正负成对(帧在落 → 不报;
真空 73.6 秒 → 报;帧回来 → 自己退回;只有 keepalive → 照报;一帧没来 → 从轮次开头算),
四条 ablation 各自只红该红的那几条。

### ③ codex 写文件不落行(daemon,已修)

codex 发的 `item.started` / `item.completed` 里 `type: "file_change"` **整条没被解析**,
以 `raw` 丢掉 —— 所以 codex 写文件从来不出「新建 xxx.html」那一行,而同样的活
claude 会出。39 个录制里 5 个中招;顺带确认 `file_change` 是**唯一**还没解析的 raw 形状。
分支 `fix/codex-file-change-rows`。

### ④ 三条 todo 划线且不能展开 —— **不是 bug,是如实反映**

用户:「为啥这里 claude 的这几个 todo 都没法展开啊?好像啥内容没有就直接下一个了?」

规格把划线定义成「这一条不是本轮新开的活」,三种情况之一是
「③ 本轮开出来但**一次都没干过**(已关闭且名下无内容,D35)」。核对 `recalled=false`,
走的就是③。

数据完全对上 —— **claude 把活全在第一条 todo 底下干完,再 2 秒内把后三条从
`pending` 直接翻成 `completed`,中间从没进过 `in_progress`**:

```
10:25:08  锁定 Swiss minimal → in_progress
10:27:26 ~ 10:29:28   全部 Bash(内容因此都归到这一条,4m 34s + 箭头)
10:29:31  锁定 → completed,同一秒 撰写正文 → completed
10:29:32  构建页眉 → completed
10:29:33  组装正文版式 → completed
```

所以「没内容 + 不能展开 + 划线」三个信号说的是同一件事:这三步没有单独做过。
**要追的是 agent 为什么这么用 todo,不是前端。**

### 顺带记一条工程判据

`pnpm typecheck` 绿**不代表** `apps/daemon` 能构建 —— build 走的是另一份
带 `noUncheckedIndexedAccess` 的 tsconfig。这一轮合完分支后 web 全量 7351 条全绿、
根 typecheck 干净,而 `tools-dev` 起不来,因为 daemon build 挂了 6 条
(正则捕获组解构成 `string | undefined`)。**改过 `apps/daemon/src/**` 必须单跑
`pnpm --filter @open-design/daemon build`。**

## F-13 真机端到端验收结果(2026-08-27,浏览器 + 真 daemon)

运行时:`tools-dev run web --daemon-port 17456 --web-port 17573`,worktree `od-wt-chat-panel`。
截图存于 `/tmp/od-shots/`。

### 已在真机上眼见为实

| 项 | 证据 |
|---|---|
| 产物卡出现时机 | 「你好」那一轮:正文 → 「已完成 09:28」→ 下一步建议,**产物大卡不再出现** |
| 壳头耗时 | `已完成 13s` / `进行中 26s` / `进行中 1m 14s` 均正常走秒 |
| thinking 折叠(N2) | 9 个「思考过程」折叠行,**默认收起**,点开读得到完整推理 |
| 表单间距(N3) | 量到左右各 **11px**、下划线到底栏 **16px** —— 与稿子逐字一致(原来是 1px / 0px) |
| 回到最新 | 短会话 `canScroll:false` 时按钮**不出现**;长会话可滚时正常出现 |
| **claude todo** | `TaskCreate=31` → 合成 **4 个 TodoWrite 快照** → 界面渲染「执行计划 · 4 步」+ 四条清单行 |
| **#39 中断状态行** | 文案「已手动停止」✅ 颜色 `rgb(92,92,92)` = `--text-muted` ✅ 只有「继续剩余任务 / 从这里分叉」、**无赞踩** ✅ 秒数按 W4 停在 `1m 14s` |
| BYOK | OpenRouter 配好后「已连接。2119 毫秒响应 — 'ok'」;真跑一轮走 `byok-opencode`,正常出文本与 usage |
| 错误卡 | dsh 失败时正常呈现:红色标题 + 说明 + 查看详情 + 联系支持/导出日志/重试 |

### 单测验过但**真机未触发**(如实记录)

- **S12 静默计时修复**:真机跑到 `1m 14s` 时头仍是「进行中」,但那是因为工具事件本来就带时刻、
  一直在刷新计时 —— **没有验到修复本身**(它管的是「事件一直来但都不带时刻」)。
  需要一轮 >60s 的纯 thinking 才能复现,无法按需构造。
- **claude 跨回合 todo id 错位修复**:这一轮 `TaskUpdate=0`(在它推进前就手动停了),没触发。

### 真机挖出的新问题

1. **claude 的推理对当前世代模型根本不下发** —— 对照实验:
   `haiku` → `thinking_delta=15 带内容=14`,尾部 block 441 字;
   `opus` → `thinking_delta=0`,尾部 block 也没有。
   历史录制同样双峰:5 个有内容的 run **全部是 `model: haiku`**,其余 `default` 全空。
   所以 opus/sonnet 下推理窗口是空的**属于上游行为**,不是我们吞了。
   (`claude-stream.ts` 那条「空 delta 不算流过」的修复方向仍正确,救的是
   haiku 那种「delta 空、尾部有内容」的形态,留着。)
2. **紧凑 chip 在 BYOK 下念错执行者**(已修):`aria-label` 是「Claude Code · gemini-2.5-flash」,
   实际跑的是 `byok-opencode`。非紧凑那支 `chipPrimary` 早就按 `config.mode` 分了岔,
   紧凑那支 `chipAgentLabel` 没有。可见文字只有模型名,所以只有读屏用户被念错。
3. **错误卡承诺了原始报错却不给**(已派单):卡上写「原始报错收在下面的『查看详情』里」,
   展开只有 `DSH_PROFILE_MISSING_RESULT` 和几个 id;而 stderr 上那句
   `credentials-local: the value for "version" in ~/.dsh/.credentials.yaml must be a string`
   **已被 daemon 捕获并落盘**,只是从没露出来。
4. **本机 dsh 环境坏了**(非 OD 问题):`~/.dsh/.credentials.yaml` 的 schema 与
   dsh 0.1.0-rc.6 对不上(`version` 要字符串,改完 `refs` 又要字符串)。
   已还原用户文件不动,留给用户决定是否重建凭证。

### daemon 全量测试判据

3 条失败:`project-upload-filenames` 与 `run-failure-telemetry-smoke` **单跑即绿**(并行抖动);
`chat-project-skill-critique-label` 是**已知的分支落后 main** 导致,与本轮改动无关。

## F-14 第二轮真机验收(2026-08-27 下午,合并六个 subagent 分支之后)

运行时重启过(daemon 走 tsx 不热更,不重启验的是旧代码)。截图在 `/tmp/od-shots/`。

### 滚动跟随 —— 三条全部真机验通

用户原话两条:「这个总是在不该出现的时候出现..感觉稳定性太差了」+
「agent 在快速流式输出内容时,每次输出就会自动回到最底,整个对话框连向上滚动都不行」。

| 行为 | 实测证据 |
|---|---|
| 流式中往上滚 → **位置钉住** | 停在 `scrollTop=0`,30 秒流式输出期间**一次都没被拽回**(修复前每个 chunk 都拽回底部) |
| 滚不动 → **浮标不出** | `canScroll:false` 时 `.chat-jump-btn` 挂载但不带 `-active` |
| 距离不够 → **浮标不出** | 离底 363px < 门槛 406px(`clamp(clientHeight×0.75, 320, 1200)`),不亮;涨到 423px 才亮 |
| 点浮标 → **落底且跟随恢复** | 点击后 `dist=0`、浮标自收;随后内容 964→1025(+61),`scrollTop` 424→484(+61),**离底恒为 0** |

根因(subagent 查出,值得记):**跟随状态从位置推导,而跟随动作又把位置写回底部** ——
闭环。40px 的触控板轻扫还没累积过 80px 阈值,下一个 chunk 就把它拽回去了。
**不是阈值调参问题,任何阈值都不成立**,因为被测量的量正被它所控制的动作覆写。

顺带纠正一条我给出的错误建议:我让它考虑 `overflow-anchor: none`,**恰恰不能关** ——
原生 scroll anchoring 正是「上方内容重排时保持阅读位置」的机制;要做的是别把它
**误读成用户滚动**(判据:`scrollHeight` 不变才算用户动作)。

### 未能真机验到的(如实记录)

- **真实滚轮事件穿不过 open-browser-use 的桥接**(`Input.dispatchMouseEvent` 的
  `mouseWheel` 不产生滚动)。上面「位置钉住」和「跟随恢复」都是用程序化滚动 +
  真实点击验的;**纯手势路径(触控板/触屏)只有单测覆盖,没有真机证据**。
- **Upgrade 按钮的绿墨**:规则层已验(消融验红、typecheck 绿),但需要一个余额为 0
  的会话才看得见,这一轮没构造出来。机制本身是早先真机量到白色坐实的。

### 工程判据(第二次踩,已记忆)

合入动过 `packages/contracts/src/**` 的分支后**必须重建它的 dist**,否则:
typecheck 报「类型不存在」、运行时报「不是函数」。第二次尤其骗人 ——
git 报 0 冲突、合并语义我也核对过是对的,结果 8 条测试红,差点回滚一个正确的合并。

## F-15 思考是壳内的一个条目,不是壳的一种形态(2026-08-27 用户裁决)

> ⚠️ **本节「缩进」那一小段当天晚些时候被 §F-18 推翻,别照它改。**
> 本节把顶层的思考那一格补到 **22px**(和当时的顶层工具行对齐),并把它从步骤链上
> 排除掉(`:not(.thoughts)`)。用户随后指出**顶层那 22px 本身就是错的**
> (「todo 外面的工具调用应该也没这个缩进吧?」),交付稿也证实顶层只有第 0 列 ——
> 于是顶层三种行一起挪回 0,思考那一格回到链上。**现行口径以 §F-18 为准。**
> 本节其余部分(思考是壳内的一个条目、两态同一只 15px 图标槽、跑完收起、
> 真实数据与 `estimated_tokens` 的调查)照旧有效。

### 用户原话(三条,先后到达)

> 诶卧槽这个思考中的怎么把原本的进行中卡片给顶掉了卧槽..
> 这个思考中的我理解应该是嵌入到进入中卡片呢? 它就算做一个普通的文本或者工具调用,
> 只不过有一个特殊的动画和样式啊, **绝不能 thinking 的时候直接把进行中或原本的东西给替换了**啊!!
> 然后 thinking 完了之后, 怎么没像创建\编辑之类的调用那样, 把超长的内容收起来??? 全摊开了.. 太长了..

> 我说的思考完之后, 不是这个绿的, 就变成普通的这个搜索一样的东西, 只不过可以下拉展开,
> 它的缩进看是否在 todo 或者前面是否是普通文案动态决定缩进是多少, 你可以给这个加一个 brain 的 icon

> 思考中的时候, 最好是能有现在那个动画加思考中的文案, 然后下面文字也是要滚动的,
> 思考完就收起变成 toolrow, 但思考中动画啥的, 这些都是和 toolrow 所在地方同样的缩进逻辑

### 裁决

思考在壳里**占一个条目**,和工具行平级,两种形态:

| 形态 | 长什么样 | 缩进 |
|---|---|---|
| 思考中 | 球(`composing`)+ 扫光「思考中」+ 三个点;正文走 96px 限高窗、自己一行行往上走 | 与同位置的 `ToolRow` 一致 |
| 思考完 | brain 图标 + 「思考过程」一行,默认收起,点开读细节;**不画绿勾** | 同上,两态之间左边缘不跳 |

壳 body 永远是 `.stack`;壳头永远说「进行中」。

### D46 判定:**规格没写错,是实现读错了**

`chat-panel-next.md:445` 的 D46 原文是「落点仍在执行记录壳内(D29 不变),即**壳内的思考正文区**
限高 + 自动贴底」——「壳内的思考正文区」本来就指壳里那一段推理,不是整只壳 body。
实现(`ExecutionShell.tsx` 的 `streaming = running && shell.thinking`)把它套在了整只 body 上,
而那个值同时还关掉了分组、还挂了整只 body 的自动滚动,三件事一起作用在整张壳上。

所以 **D46 不需要推翻,只需要按原文落对**;本轮新增的是它没写过的部分:
思考块自己的身份(头部动画 + 文案)、与 ToolRow 对齐的缩进、跑完收起。
`chat-panel-next.md` 的 D46 行已加指针指向本节。

### 真实数据(问题二的证据,不是构造夹具)

本机 `.od/runs` 共 63 条真实 run(2026-08-26~27,即用户当时在跑的那个工作树),
其中 34 条带 `thinking_delta`,合计 15,905 帧:

| agent / model | 运行数 | thinking 帧 | 非空帧 | 推理字数 |
|---|---|---|---|---|
| amr | 1 | 12,763 | 12,231 | 42,097 |
| claude · default | 14 | 1,786 | **0** | 0 |
| claude · haiku | 4 | 132 | 132 | 3,630 |
| claude · 未记录 | 15 | 1,224 | 67 | 2,166 |

两个结论:

1. **claude(default)的 thinking 全是空串** —— 与仓库内真实录制夹具
   `claude-shop.turn0.json`(247/247 空)一致。所以「思考中」这个态**不能以有没有文字为前提**,
   否则 claude 思考几十秒界面上一点反应都没有。
2. **推理文字是真的会很长** —— run `0161ef44`(agent=amr)一轮 42,397 字。
   把它喂进渲染实测:**38,064 字铺在屏幕上**(全在那条 in_progress 的 todo 抽屉里),
   只有 4,333 字被顶层的「思考过程」收住。

真因:`ExecutionShell` 只对 `shell.items` 调 `groupThinking`,`TodoRow` 直接
`segment.items.map(renderItem)` —— **抽屉里那一摞从来没分过组**。本轮有清单时推理就落进
当前 todo(`build-turn-blocks` 的 `sink()`),于是一个字都收不起来。

夹具 `apps/web/tests/fixtures/chat/amr-thinking-todo.turn0.json` 就是那条 run,
只把连续的 thinking delta 拼起来(`buildTurnBlocks` 本来就会拼,已逐字节验证落块结果相同),
其余事件原样。

### `estimated_tokens` 走不通(查过,如实记录)

上游 claude 的空 thinking 帧带 `{"thinking":"","estimated_tokens":null}`,曾被提议当进度量。
**实测不可用**:本机 100+ 条真实 run 日志里 `estimated_tokens` 出现 **0 次** ——
`claude-stream.ts` 只读 `delta.thinking` 并发 `{type:'thinking_delta', delta}`,
这个字段在 daemon 边界就被丢掉了,从未进入持久化事件流;代码库里也搜不到任何引用。
要用它得新开 daemon → contracts → web 一整条字段,而唯一观察到的上游取值还是 `null`。
**本轮不做**,不拿安慰剂顶替真实进度。claude 的「有反应」由思考块自己的动画承担
(球 + 扫光 + 三个点,一个字都没有也照转)。

> **2026-09-04 更正:上面这段结论对了一半,量错了地方 —— 现已落地。**
>
> 那次量的是 `thinking_delta` **载荷里**那个 `estimated_tokens`,对**那个字段**结论完全成立:
> 每个 thinking 块的收尾帧必然是 `null`,而且非 null 时它是**每帧增量**不是累计
> (`partial-single-turn` 第二块:累计 50/150/300/450,同批 delta 报 50/100/150/150),
> 不开 `--include-partial-messages` 时更是一帧都不存在。
>
> 漏看的是**另一种帧**:CLI 还单发
> `{"type":"system","subtype":"thinking_tokens","estimated_tokens":N,"estimated_tokens_delta":N}`。
> 六份真实录制里 **36 帧全部非空**,两种 CLI 配置下都在 —— 而
> `claude-stream.ts` 的 `handleObject` 认得 `system/init` 与 `system/status`,
> 唯独这一种直接落到底,所以它**从来没进过日志**,「日志里 0 次」量到的正是这个。
> 语料与判据钉在 `apps/daemon/tests/runtimes/w134-thinking-token-count.test.ts`。
>
> 落地形态:思考行右边那个槽(`ThoughtsRow`),**一个槽、一个数**,
> 报此刻还活着的那件事 —— token 在动就写 token,停够 8 秒就把槽让给计时;
> 头一格没有计时可让(今天刚因「与壳头重复」收走),所以那一格恒为 token。
> 不是安慰剂:这个数**恰恰在正文永远不会来的那一档存在**(真机 CLI 2.1.260:
> 3060 个计费 token、0 个字符)。球 + 扫光 + 三个点一件没动。
>
> ⚠️ 同一份录制里还有第三个数 `usage.output_tokens_details.thinking_tokens` ——
> 那是**结算**值,块跑完才有,而且比估算值低 20–60%。屏幕上要的是块**跑着时**的进度,
> 那一刻结算值不存在;等它到了,那一格也收掉了。两个数不可互换。

> **2026-09-06 更正:上面「那一格恒为 token」这句话的前提塌了 —— 现已落地。**
>
> 写下那句话时的默认是「头一格没有计时可让」。它在**两处**不成立:
>
> · **AMR 没有 token 可恒。** `thinking_tokens` 全仓只有两个生产者
>   (`runtimes/claude-stream.ts` 的 claude、`agent-protocol/codex-app-server/normalize.ts`
>   的 codex);ACP 那条路(`agent-protocol/acp/session.ts`)一个都不发。
>   实测本机 beta 数据目录 25 条 `agentId=amr` 的 run,该事件出现 **0 次**
>   (同期 claude run 每轮 22–2537 次)。于是那一格两半都空:token 没有,计时被压着。
> · **claude 自己在收尾之后也拿不到。** token 只挂给还活着的那一格
>   (`group-thinking.ts` 的 `if (live)`),`build-turn-blocks` 更是只发给
>   `block.thinking === true` 的壳。回合一结束两者同时为假,头一格退回空槽。
>
> 用户 2026-09-06 逐字裁决:「即使是第一个 thinking,思考过程中不显示耗时,
> 但**结束还是要显示的吧**?」—— **思考进行中不显示耗时(维持现状),但收尾之后要显示。**
>
> **这不是推翻 2026-09-04,是给它补上到期时刻。** 收走计时的理由是它和壳头
> 「同起同终、写的是同一个事实」,而那只在这一格**还在流**的时候成立:一旦下一件
> 带时刻的事把它结账(`build-turn-blocks` 的 `stamp()` → `closeThink(at)`),
> 这一格的数字就冻住,壳头那个数继续走到轮次收尾 —— 两个数当场分叉,「重复」消失。
>
> 落地形态:`ThoughtsRow` 的 `const elapsed = muted && live ? '' : formatElapsed(elapsedMs)`。
> 判据 `live` 是**这一格自己的相位**(`groupThinking` 只发给结尾那一格),和同一个组件里
> 决定「球 + 思考中」还是「brain + 思考过程」的是**同一只开关** —— 数字和形态同一帧翻面,
> 不会出现「已经写着思考过程、右边还空着」。
>
> token 那条规则**一个字没改**:两者相位不重叠(想完的那一刻 `tokens` 已是 `null`),
> 所以一格之内只切换一次、方向单向,不会在 token 和秒数之间来回闪。
>
> ⚠️ 用户同一轮报的另一条「AMR 没有 token 输出计数」**没有修**:裁决明说进行中维持现状,
> 而它的真因是 ACP 不发 `thinking_tokens`(daemon/协议侧的缺口)。在渲染层拿耗时去顶
> token 的位置等于替产品决定「AMR 的思考中改成显示秒数」,没有这条裁决不许自造。
>
> 判据:`tests/components/chat/first-thoughts-no-elapsed.test.tsx`(进行中仍然空白)、
> `tests/components/chat/amr-thinking-slot-blank.test.tsx`(想完之后要有数,含 AMR 那一档
> 维持空白的守卫)、`tests/components/chat/thinking-token-count.test.tsx`(token 规则未动)。

### 缩进:在真 Chrome 里量的(不是只 diff CSS 文本)

harness 做法:把组件真实渲染出的 DOM(CSS Module 类名去哈希)+ `record.module.css`
+ `ChatRoot.module.css` 的 token 拼成一张静态页,起 `python3 -m http.server` 用真 Chrome 打开,
读 `getBoundingClientRect()` 与 `getComputedStyle`。

行首图标的 x(相对壳左边缘):

| 场景 | ToolRow | 思考块(修前) | 思考块(修后) |
|---|---|---|---|
| 顶层 · 思考中 | 22 | −3 | **22** |
| 顶层 · 思考完 | 22 | 0 | **22** |
| todo 抽屉 · 思考中 | 22 | 19 | **22** |
| todo 抽屉 · 思考完 | 22 | 22 | **22** |

两处修:

- `.fold.flat > .body.stack > .fold.thoughts > summary { padding-inline-start: 29px }` ——
  顶层折叠头默认只有 7px(图标落 0),而同层 `div.tool` 是 `-7 + 29`(图标落 22)。
  **只补顶层**;抽屉那一层两者同为 29px,本来就齐,再补会推出去。
- 思考中的球塞进和 brain 图标同一只 `.icon`(15px、`place-items:center`)。
  球自带 `margin-inline:-3px`,直接摆在 summary 里会让思考一结束整行横跳 3px。

**反向对照**(证明 22px 不是写死的常量):

| 场景 | 夹心正文 `padding-inline-start` | 竖线 |
|---|---|---|
| 工具行 → 正文 → 工具行 | **22px** | 画 |
| 工具行 → 正文 → 思考块 | **22px** | 画(思考块照旧当「步骤」括住正文,没退化) |
| 正文打头,前面没东西 | **0px** | 不画 |
| 有清单的壳,顶层正文 | **0px** | 不画 |

另外把思考块排除出**步骤之间那条竖线**(`:not(.thoughts)`):那条线画在 14.5px,
是按「折叠头贴左」算的;思考块缩到 22px 之后它会孤零零落在图标左边。工具行本来就不画这条线。
真 Chrome 验过:思考块(非末位)`content:none`;普通折叠(非末位)照旧
`content:""` / `rgb(216,216,216)` / `14.5px`。

### 已知残留(未修,列出来)

- `.tool` 的图标→文字间距是 `gap:8px`,`.fold > summary` 是 `7px`,于是文字左边缘差 1px
  (45 vs 44)。这是 `.tool` 与**所有**折叠行之间既有的差,命令工具行同样如此,
  不是本轮引入;改它会动到壳里每一行,另开一条。

---

## F-16 逐字化开铺开到所有正文,滚动节奏改成跟着到达速率走(2026-08-27 用户裁决)

### 用户原话(逐字,两条)

> 这个下面滚动的文本的缩进感觉不对呢, 没对齐... 而且滚动速度是不是要动态一下啊,
> 现在固定的感觉不太对, ds-v4-flash thinking 输出的非常快的..

> 能不能 thinking 还是用有个流式输出的效果, 你找一下, 设计稿 就是后面出来的文本有个
> 动态模糊再逐渐清晰的效果, 包括我们所有普通文本, 都应该有这个流式输出的效果才对,
> 不能直接刷一下子整个出来..
>
> 如果不是真正流式的, 可能背后 daemon 还是一次性出来, 但展示的时候, 也保留一个流式的
> 效果, 可能完整走完这个流式输出效果 2s 左右的动画, 加速一下

### 设计稿里那个效果的完整规格(交付稿 `1bbdce0b06`,md5 `28ea4c65…`)

出处:1605–1631 行(CSS + 那段注释)、6686–6730 行(拆字脚本 + 注释)、3656 行(思考流的出处)。
照搬 motion-primitives 的 `TextEffect(per='char')`(@ibelick,21st.dev)的**进场那一半**:

| 项 | 值 | 稿子原话 / 位置 |
|---|---|---|
| 单字时长 | `0.4s var(--ease-out)`,`animation-fill-mode: both` | 1622–1624 行 |
| 字间错开 | `--i * 0.01s` = 10ms | 1624 行,`--i` 由拆字脚本写 |
| 起 | `opacity: 0; filter: blur(10px) brightness(0%)` | 1627 行 |
| 止 | `opacity: 1; filter: blur(0px) brightness(100%)` | 1628 行 |
| 为什么是 brightness | 「配着 blur 一起,字是从一团化不开的墨里浮出来,而不是单纯地由淡变浓」 | 1609–1610 行 |
| 只进不退 | 「这段话是这一轮交给人的结论,出来了就该一直在」 | 1611 行 |
| 拆字粒度 | 元素子节点**整块算一个单位**,不钻进去拆(拉丁词不能被劈开) | 6702–6706 行 |
| 空白 | **不裹** —— 包成 inline-block 会吃掉断行位置 | 1614–1616 行 / 6722 行 |
| reduced-motion | 全局 `* { animation-duration: 0.01ms !important }`(489–490 行)+ `fill-mode: both` ⇒ 直接停在末帧,是完整的一段字不是空白 | 1617–1618 行 |
| **不做** `startOnView` | 上游有这个选项,稿子试过后撤掉:观察器不回调时整段停在 `opacity:0`,一片空白 | 1619–1626 行 |
| 一段五十来字 | 约 0.9s 化完(0.5s 错开 + 0.4s 最后一个字) | 1607–1608 行 |

**稿子把这个效果只挂在 `.say` 上**(4494 / 4961 / 4965 行三处 `data-reveal`),
思考流那一段 `.think` **没有**挂。「思考也要有」是用户这一轮新加的,不是稿子里的。

**稿子没有「整段总时长」这个量。** 0.4s 是单字时长、10ms 是字间隔,两者都不封顶整段。
一段 34,731 字按 10ms 排要 347 秒。用户给的 2s 是**总时长目标**,和稿子的 0.4s 不是一回事。

### 实现:预算封顶单位数,而不是压间隔

`useCharReveal.ts`:`MAX_UNITS = (2000 - 400) / 10 + 1 = 161`。
一批待化开的字先按 161 封顶,装不下就**把单位加粗**(一个 span 装多个字)。于是

- 错开值永远是稿子的 10ms、单字时长永远是稿子的 0.4s —— 两个原值一个没动
- 总时长 `(units - 1) × 10ms + 400ms` 天然 ≤ 2000ms
- 五十来字的一句话仍然是 `49 × 10 + 400 = 890ms`,和稿子那句「约 0.9s」对得上
- DOM 一次最多 161 个 span,不是一字一个;同时在开的只有 `400 / 10 = 40` 个

**多字单位用 `display: inline` 而不是稿子的 `inline-block`。** 稿子那句
「inline-block 是 filter 生效的前提」在单字下成立,但一个装 216 字的 inline-block
是一整块不可断的长条,拉丁正文会被它顶出行外。真 Chrome 量过:`filter` 在
`display: inline` 上照样解析(`blur(4px) brightness(0)`)、照样立层叠上下文,
而且 span 会正常裂成多个行框(`getClientRects().length === 6`)。单字单位仍走 inline-block。

### 铺开到哪几处

| 位置 | 挂点 | 判据 |
|---|---|---|
| 壳外正文(助手的回答) | `AssistantMessage` 的 `proseRef` | `isLastAssistant && streaming`(本来就有,这一轮修好了它) |
| 思考流 | `ThoughtsRow` 的 `bodyRef`(整只 body) | `live` |
| 壳内过程叙述 | `SayText` 的**整只 `.think` 容器** | 这一摞的最后一个 `text` 条目,且这一轮在跑 |

~~叙述那边挂在最后一段 `<p>` 上~~ **2026-09-03 改成整只容器**。原来的写法(挂最后一段)
在纯文本下成立:新起一段时那只 `<p>` 是全新元素,自己从头化开,不必交接。
壳内文字改走 markdown 之后这个前提没了 —— 块树的元素会**换身份**(`#` 再来一个字就从
`<p>` 变成 `<h2>`),每换一次 `useCharReveal` 按元素记的状态就丢一次,已经看过的字
被当成新字重放。挂到容器上则整块共用一份 `shown`,和思考流那边同一种挂法。
判据见 `apps/web/tests/components/chat/say-text-markdown.test.tsx`。

### 修前的真实基线(无头 Chrome 量的,不是推测)

| 场景 | 修前 `.rv` span | 修后 |
|---|---|---|
| 一次性到达 2,000 字(壳外正文) | **0**(整段瞬间刷出来 —— 用户说的那一下) | 161 |
| 每帧 10 字增量(壳外正文) | 10 | 10 |
| 思考流增长 | **0**(从来没接过) | 有 |

真因:`useCharReveal` 里 `const added = !sameNode ? 0 : …` —— **第一次见到一个文本节点时
一个字都不化开**。原注释写着这是故意的(「整段一起闪更糟」),而有了 2s 预算之后,
一次性到达不再是「一起闪」,是 2s 内铺完,所以这条前提没了。

### 性能(无头 Chrome,`--headless=new`,120Hz,同一次会话内对照)

| 场景 | 峰值 span | 帧 p50 | 帧 p95 | >20ms | >33ms |
|---|---|---|---|---|---|
| 空转基线(什么都不动) | — | 8.3ms | 8.7ms | 0 | 0 |
| **中文** 34,731 字一次性 | **161**(不是 34,731) | 8.3ms | 8.7ms | 1 | 0 |
| **英文** 34,731 字一次性 | **161** | 8.3ms | 8.7ms | 1 | 1 |
| 8,000 字/秒 流式到 34,731 字 | 134 | 8.3ms | 9.1ms | 0 | 0 |
| 同上 + 思考限高窗 + 自动滚 一起跑 | 134 | 8.3ms | 8.9ms | 0 | 0 |

那唯一一帧(24–41ms,跑几次在这个区间浮动)落在 React **第一次把 34,731 字写进 DOM**
的那一帧上,不是化开造成的(把化开关掉那一帧照样在)。

### ⚠️ 只有英文照得出来的两条(中文一路测不出)

**中文没有空格**,而这两条都只在有空白时发生。是拿英文在无头 Chrome 上跑
34,731 字才照出来的:

1. **空白被复制一份。** 单字单位时空白按稿子的规矩裸着放(不进 span),而 `restore()`
   原来只 `querySelectorAll('[data-char-reveal]')` 扫 span —— **裸空白删不掉**,
   再把节点还原成完整值,空白就多了一份。实测收尾字数 **40,600 / 34,731**,
   多出来的 5,869 正好是空格数。
   修:`Touched` 记一份 `inserted` 名单(span **和**裸空白),按名单删。

2. **span 按「词」爆掉。** 按空白切单位的话,英文 34,731 字拆出 **5,976 个 span**
   (≈词数),预算白封 —— 帧 p95 从 8.7ms 掉到 **83.3ms**,**29 帧超过 33ms**,肉眼可见地卡。
   修:**单位不止一个字时,空白进 span**。多字单位本来就走 `display: inline`,
   inline 盒子自己会裂成多个行框(真 Chrome 量过 `getClientRects().length === 6`),
   断行位置一个都不少;单字单位仍照稿子裸着放。
   修后 161 个 span、p95 8.7ms、`p.scrollWidth > p.clientWidth` 为 `false`(不撑出列)。

两条都有测钉着(`blur-stream-reveal.test.tsx` §8),并各配了一条「同长度的中文文本
本来就不会出这个错」的反向对照。

**空白有两条代码路径,要分开钉**:多字单位(一次性大段)空白进 span;
单字单位(小增量流式,一个词一个词地来)空白裸着放在 span 外面。
第一版只测了前者 —— 消融时把 `restore()` 那份 `inserted` 名单撤掉**仍然全绿**,
因为多字单位那一路根本没有裸空白。补了一条「一个词一个词地流进来」的用例之后,
撤掉名单当场红。**消融照出了测试的洞,不是照出了实现的洞。**

动画中途量到的分布:153 个 span 里有 **42 个不同的 filter 值**,
从 `blur(10px) brightness(0)` 到 `blur(0px) brightness(1)` —— 与「同时在开 40 个」的预测一致。

**reduced-motion 实测**(Chrome `--force-prefers-reduced-motion`):
`.rv` span **0 个**、文字一个不少(1218/1218)、思考窗 `mask: none` / 高度放开(96 → 102)
/ `scrollTop` 恒为 0(自动滚也停了)。三处降级一次量全。

**开完就撒手**:`useCharReveal` 到点自己 `restore()`,不等下一次重渲染。
Chrome 实测动画结束后 `.rv` span **归零**、`filter` 集合为空、字数一个不少(306/306)。
不做这一步的话,流最后一批字落地后可能一次渲染都没有,161 个 span 会一直挂着 filter。

### 滚动节奏:2000 从**定值**降级成**上界**

原来 `STEP_MS = 2000` / `MOVE_MS = 550` 是写死的,一字不差抄自交付稿的 `stream()`。
**这两个数没写错** —— 稿子自己的注释说得很清楚(6482–6489 行):

> 它是【演示】:真实的 Thinking 是模型边吐边把内容顶上去,不会回到顶重来。
> 稿子里没有真的在吐字的模型,只能拿一段写死的推理循环滚。

2000 是配着一段**不再增长**的死文本挑的节奏。真机上文字一直在长,节奏必须跟着到达速率走。

`planStep(growthPxPerSec, linePx)`:

| 量 | 值 | 理由 |
|---|---|---|
| 上界 | `STEP_MS = 2000` | 交付稿原值。**慢的时候一步不差还是稿子的样子** |
| 下界 | `MIN_STEP_MS = 300` | 走的那一段 = `300 × 0.275 ≈ 83ms` ≈ 60fps 下 5 帧,再短就不是「滑过去」是「跳一下」;剩下 217ms 是一行字站定被读到的下限 |
| 走停比 | `MOVE_RATIO = 550 / 2000` | 每一档速度都保持它 —— 变的是快慢,不是「走一下停一下」这个形态 |
| 一步几行 | `1…MAX_STEP_LINES = 3` | 夹到下界还追不上时才加距离。上限 3 是因为窗口只有 96px(≈5 行),稿子拒绝「按段走」的理由是「一跳半屏就没了连续感」,3/5 已经贴着那条线 |

先试过 `MIN_STEP_MS = 500`:1 行/秒 到 3 行/秒 整段被夹平成同一个值,而那正好是
ds-v4-flash 所在的区间 —— 夹平等于没做动态。`thinking-stream-tempo.test.ts` 里
「随速率连续变」那条反向对照就是钉这件事的。

到达速率用 `scrollHeight` 的增量做指数平滑(`RATE_TAU_MS = 1200`),
换档只发生在**步与步之间** —— 步进行到一半改 `stepMs`,缓动会当场跳一下。

真 Chrome 量的有效滚动速度(同一只窗、同一次会话):

| 到达情况 | 有效滚动速度 | 对照 |
|---|---|---|
| 内容不再增长 | **7.7 px/s** | ≈ 交付稿的「一行 / 2s」(18.6px ÷ 2s = 9.3,差的是回顶折返不计正向推进) |
| 每 120ms 追加一段(模拟 ds-v4-flash) | **166.2 px/s** | 21×,已贴着上限(3 行 × 18.6px ÷ 0.3s = 186 px/s) |

**注意最后那一行的含义**:那一档里内容以 ≈271 px/s 在长(1897px / 7s),
而节奏已经打满 —— 也就是说**到了极端速率,跑马灯即使跑到最快也追不上**。
这不是参数没调好,是「一步一停 + 到底回顶」这个形态本身的天花板,见下面那条待拍板。

### ⚠️ 待产品拍板:这个跑马灯滚动还该不该存在

**没有动手,按裁决权归属留给产品。** 摆出来的三条:

1. **稿子自己说它是演示的替身。** 6482 行:「真实的 Thinking 是模型边吐边把内容顶上去,
   **不会回到顶重来**」。我们现在做的「到底了回到顶」正是稿子说真机不会有的那个行为。
2. **它和逐字化开在说同一句话。** 稿子撤掉行尾光标的理由(1633–1643 行)是
   「这一屏已经有两处在说同一件事…文字本身也是一个字一个字浮出来的。第三个记号不添新信息」。
   同一条推理对跑马灯同样成立:化开已经表达了「还在写」。
3. **动态节奏只能缓解,不能治本。** 循环回顶意味着窗口周期性地退回去展示**旧文本**;
   跟不跟得上到达速率是另一回事。真要「跟得上」,正确形态是**贴着最新内容走**(日志尾巴那种),
   那时节奏自动等于到达速率,不需要 `planStep`。

建议形态:思考期间**贴底跟随最新推理**,停止到达后再恢复(或直接不动)。
真要改的话 `useThinkingStream` 改动很小,但**这是产品决定**。

---

## F-17 思考正文的左缘 + 思考过程展开限高(2026-08-27 用户裁决)

### 用户原话(逐字,两条)

> 这个下面滚动的文本的缩进感觉不对呢, 没对齐...(配图:思考中那一档,推理正文明显比头里的文字更靠左)

> thought 展开应该有个最高高度, 可以滚动(配图:思考已结束那一档,展开后十几段一路铺到屏外)

### 缩进:真因是**特异性**,不是缺一条规则

§F-15 把思考从「壳的一种形态」改成「壳里的一个条目」之后,那只 96px 的窗从
**壳自己的 body** 挪到了 `.fold.thoughts` 的 body 上。于是这条:

```css
.fold.flat[open] > summary + .body.stream { border-top: 0; padding-top: 0 }
```

**整条失配** —— 它要求这只窗是扁平壳的直接 body。窗子换了挂点之后,层叠里剩下的是:

| 规则 | 特异性 | 结果 |
|---|---|---|
| `.fold .body { padding: 0 11px 11px }` | (0,2,0) | 赢 |
| `.fold[open] > summary + .body { padding-top: 4px }` | (0,3,0) | 再赢它 |
| `.stream { padding: 0 }` | **(0,1,0)** | 两条都打不过 |

交付稿对应的那条是 `.fold.mod-flat > .body.mod-stream`,(0,4,0) —— **稿子本来就够特异**,
是我们这边写成了裸 `.stream` 才输掉的。

真 Chrome 量的(坐标相对壳 body 的内容盒左缘;harness 是把真组件挂进 vite,
连 `src/index.css` 整条 import 顺序一起加载,不是拼静态页):

| 场景 | 行首图标 x | 头里文字 x | **正文左缘 x** | body padding |
|---|---|---|---|---|
| 思考中 · 顶层(修前) | 22 | 44 | **4** | `4px 11px 11px` |
| 思考中 · 抽屉里(修前) | 22 | 44 | **4** | `4px 11px 11px` |
| 思考完 · 顶层(修前) | 22 | 44 | 22 | `2px 0px` |
| 思考完 · 抽屉里(修前) | 22 | 44 | 22 | `2px 0px` |

也就是说 **两态错开 18px**:思考中的正文跟谁都不对齐,而且思考一结束整段字横跳 18px 回到 22。

两条修:

```css
/* 特异性补到和稿子同档,并且不再要求祖先是扁平壳 */
.fold[open] > summary + .body.stream { border-top: 0; padding: 0; }
/* `.body.stream` 走和 `.body.stack` **同一条**缩进 */
.fold .body.stack :is(.body.stack, .body.stream) > * { padding-inline-start: 33px }
.fold.flat .body.stack :is(.body.stack, .body.stream) > * { padding-inline-start: 29px }
```

修后四档全部 **正文左缘 = 行首图标 x = 22**,与同层 `div.tool` 的图标同列。
不变量是**「正文左缘 == 这一行自己的图标列」**,不是「22」这个常量 ——
四个场景里 22 是同一个数,但它是被 `-7 + 29` 算出来的,反向对照见下。

**反向对照(证明不是写死 22):**

| 撤掉哪一条 | 思考中正文左缘 | 思考完正文左缘 |
|---|---|---|
| 不撤(基线) | 22 | 22 |
| 撤掉 `padding` 那条(退回 `.fold.flat[open] …`) | **33** | 22(不受影响) |
| 撤掉 `.body.stream` 的缩进(只留 33px 那半) | **26** | 22(不受影响) |

每一撤只动它该动的那个数,另一档纹丝不动。

### 展开限高:值来自稿子,不是我挑的

交付稿 1252 行:

```css
.fold .body.mod-scroll { max-height: 96px; overflow-y: auto; }
```

3674 行那条注释把这个数说死了:「96px ≈ 5 行,和折叠块里 `.mod-scroll` 用的是同一个高度,
**这一屏里所有『够看但不占屏』的窗口都是这一档**」。`.mod-scroll` 在稿子的标记里
一次都没用上(只声明了规则),所以稿子给的是**规则和值**,没给用它的那一格。

和思考**中**那只窗是两回事,别合并:

| | 思考中 `.stream` | 思考完 `.scroll` |
|---|---|---|
| 高度 | `height: 96px` **定死** | `max-height: 96px`,短内容不限高 |
| 遮罩 | 上下渐隐 | **无** —— 遮罩会把正在读的那一行切掉半截 |
| 谁在滚 | 自己往上走 | 用户自己滚(`overflow-y: auto`) |
| 为什么 | 「它还在想」这件事本身 | 用户**专程点开来读** |

⚠️ `.stream` 用的是 `height` 不是 `max-height` —— 读 computed style 时别被
`max-height: none` 误导。

`Foldable` 原来的 `scroll?: { maxHeight: number; stickToBottom?: boolean }` **全仓零消费方**,
`stickToBottom` 也没有任何实现。改成 `scroll?: boolean`,高度回到 CSS(稿子就是这么放的)。

真 Chrome 量的:

| 场景 | 内容真高 | 渲染高 | `max-height` | `overflow-y` | 出滚动条 |
|---|---|---|---|---|---|
| 思考完 · 14 段(长) | 429 | **96** | 96px | auto | **是** |
| 思考完 · 一句话(短) | 34 | **34.4** | 96px | auto | **否** ← 反向对照 |
| 思考中(不该被波及) | 96 | 96 | **none** | hidden | 否 ← 反向对照 |
| 撤掉 `.scroll` 之后的长内容 | 429 | **429.5** | none | auto | 否 ← 消融 |

### ⚠️ 96px 这个数值请产品过目

规则和值都照稿子搬的,但稿子从没把 `.mod-scroll` 用在「用户点开来读的一长段推理」上。
96px ≈ 5 行:治住了「铺满整屏」,但要读十几段推理时是个很小的窗口。
**改它只是一个数**(`record.module.css` 的 `.fold .body.scroll`),没有连带影响。

### 已知残留(未修)

- `.tool` 的图标→文字间距 `gap:8px` vs `.fold > summary` 的 `7px`,文字左缘差 1px(45 vs 44)。
  §F-15 已记,不是这一轮引入的。

---

## F-18 壳里只有两套列,链穿过顶层的每一格(2026-08-27 用户裁决)

### 用户原话(两条,先后到达)

> 这里衔接咋是这样的?

配图是一只跑完的壳:线沿着上面那条命令步骤的正文走下来,走到「思考过程」那一格
**断掉**,下面的步骤再重新起一条。

> 这里不是说要动态判断间距吗? 像这里如果是 todo 外面, 不应该有这个缩进?
> todo 外面的工具调用应该也没这个缩进吧?

配图红框圈住**顶层 Thoughts 左边那个空槽** —— 它比同一只壳里的 `Plan · 3 steps`
右了一格。

**两条是同一个病**:顶层的工具行 / 思考那一格被缩进了 22px,而链的 x 又是照
「顶层贴左」写死的 14.5px —— 于是它们既比邻居右了一格,又接不上那条线。

### 去交付稿里核过的三件事

材料:`docs/design/chat-panel-next.html`(修订 `1bbdce0b06`,md5 `28ea4c65…`),
放进无头 Chrome 逐行量 `getBoundingClientRect()`。

**1. 稿子里有没有「壳里没有清单、工具行直接平铺」的格子?没有。**

`.fold.mod-flat > .body.mod-stack > .tool` 在稿子自己的 84 格里命中 **0 处**。
组件 9 / 10 / 11 / 12 那几格单独演示工具行时,外面照样套着一层 `details.fold`
(带 `.mk` 状态点的**步骤**),工具行住在它里面:

```
div.fold.mod-flat                  ← 壳
  div.body.mod-stack
    details.fold[open]             ← 步骤(.mk is-ok「复刻商品列表页」)
      div.body.mod-stack
        div.tool ×N                ← 工具行永远在这一层
```

所以我们那条 `.fold.flat > .body.stack > .tool { padding: 5px 7px 5px 29px }`
**抄的值没错、位置错了**。它的注释也写明了适用前提:「折叠头本身就是一个步骤时,
它下面的工具行是这个步骤的子项」。而我们的壳头永远是「进行中 / 已完成」,
从来不是一个步骤 —— 顶层的工具行没有「谁的子项」可言。

**2. 顶层和步骤里面是不是两套列?是。**(坐标相对壳 body 内容盒左缘)

| 稿子里的这一行 | 行首那一格 x | 宽 | 名字 x | 链 x | 链 top |
|---|---|---|---|---|---|
| 顶层 `details.fold`(步骤)的 `.mk` | **0** | 15 | 22 | 7.5 | 25px |
| 步骤**里面**的 `div.tool` 的 `.ti` | **22** | — | — | 不画 | — |
| 步骤**里面**的 `div.tool` 的 `.pk`(序号) | **22** | — | — | 不画 | — |
| 步骤**里面**嵌一层的 `details.fold` 的 `.ti` | **22** | — | — | 不画 | — |
| 夹在两步中间的 `p.think` | — | — | 22 | 7.5 | **0px** |

**3. 那 22px 从哪来?** 稿子原话:「缩进一格(**状态点 15 + 间距 7 = 22**)」——
让开步骤自己那颗状态点。所以它是「**步骤里面**」的列,不是「工具行」的列。
用户那句「是不是绿勾占一格所以里面的东西要让开」正是这个。

### 裁决(规则,写成两条)

| 层 | 谁在这一层 | 行首那一格 x | 画链吗 |
|---|---|---|---|
| **顶层**(`.fold.flat > .body.stack` 的直接子代) | 步骤、`div.tool`、思考那一格、清单抽屉 | **0** | 画(最后一格除外) |
| **抽屉 / 步骤里面** | 子工具行、子折叠块、抽屉里的思考 | **22** | 不画 |

两条推论:

- 顶层三种 DOM(`details.fold` / `div.tool` / `details.fold.thoughts`)**取同一个
  内边距**(`5px 7px`),思考那一格不再有专属规则 —— 有专属规则就有各走各的余地。
- 顶层正文(`p.think`)照旧贴左;**有清单的壳**里夹在两条抽屉中间的正文也贴左
  (§F-11 的裁决原样保留,只是「正文」这条现在推广成了「顶层的每一种行」)。

### 链的 x 不再写死

原来两处各写各的数:链那条 `14.5px`、夹心正文那条 `7.5px`。它们**必须**落在同一条
绝对轴上,差的 7px 只是因为两种盒子往左探出的距离不同。写成两个常量的直接后果就是
这一轮的 bug:思考那一格换了缩进,`14.5` 没跟着动,于是当时用 `:not(.thoughts)`
把它从链上摘掉,链就断在那一格。

现在只有一个表达式:

```css
.fold.flat > .body.stack        { --row-slot: 15px; --chain-x: 7.5px; --row-outdent: 0px; }
.fold.flat > .body.stack > :is(.fold, .tool) { margin-inline: -7px; --row-outdent: 7px; }
/* 两处共用 */                   inset-inline-start: calc(var(--chain-x) + var(--row-outdent, 0px));
```

`--chain-x` 是 `--row-slot` 的一半 —— 链的中轴就是行首那一格的中轴,两个数写在一起,
哪天那一格换了宽度,线跟着走,不会悄悄偏出半格。

另外链的首尾判据从 `:not(:last-of-type)` / `:not(:first-of-type)` 换成
`:has(~ :is(.fold, .tool))` / `:is(.fold, .tool) ~ .fold[open]`:稿子顶层清一色
`details`,`of-type` 够用;产线上顶层混着 `div.tool`,`of-type` 按标签各数各的,
一条尾随的工具行不会让前一条 `details` 失去 `:last-of-type`,链会凭空多一截或少一截。
(同一处修正夹心正文那条规则 §F-11 已经做过。)

### 真 Chrome 量的(改前 → 改后)

harness:`apps/web/tests/components/chat/record-columns-probe.test.tsx` 把真组件渲染成
一张自包含 HTML(CSS Module 类名去哈希 + 内联 `tokens.css` / `base.css` 的 `:root` /
`ChatRoot.module.css` / `record.module.css`),再用无头 Chrome 的 CDP 读坐标 ——
做法照 `docs/design/chat-mirror/measure.mjs`。坐标相对壳 body 内容盒左缘。

**顶层(没有清单的壳)**

| 行 | 行首 x 改前 | 改后 | 链 改前 | 改后 |
|---|---|---|---|---|
| `details.fold.thoughts`「思考过程」 | 22 | **0** | 不画 | **7.5** |
| `details.fold`「List project workspace」 | 0 | 0 | 7.5 | 7.5 |
| `div.tool`「读取 a.png」 | 22 | **0** | 不画 | **7.5** |
| `div.tool`「读取 b.png」(末位) | 22 | **0** | 不画 | 不画(末位) |
| 夹心 `p.think` | 内容 22 | 内容 22 | 7.5 | 7.5 |

**清单抽屉里(反向对照 —— 一格没动)**

| 行 | 行首 x 改前 | 改后 |
|---|---|---|
| 抽屉里的 `div.tool` | 22 | **22** |
| 抽屉里的 `details.fold.thoughts` | 22 | **22** |
| 抽屉里嵌一层的 `details.fold` | 22 | **22** |

顶层 0 ≠ 抽屉 22,而且各自内部三种行一致 —— 这一对不等式就是「没把两套列拉平」的
判据;只断言「顶层是 0」的话,把所有行一律改成 0 也能绿。

**六条不变式的复验(全部真 Chrome)**

| # | 不变式 | 证据 |
|---|---|---|
| 1 | 夹心正文对齐 | 没清单:内容 22 + 链 7.5;**有清单**:内容 **0**、不画(两种壳都量了) |
| 2 | 思考中 / 思考过程同一个 15px 图标位 | 顶层两态都是 x=0 / w=15;抽屉里两态都是 x=22 / w=15 |
| 3 | 思考正文左缘 | 四种场景(顶层 / 抽屉 × 思考中 / 思考完)都是 **22**,与改前一字不差 |
| 4 | 步骤折叠头贴左 7px | `padding: 5px 7px` 未动,量到行首 x=0 |
| 5 | 顶层 vs 抽屉两套列 | 见上面两张表:0 vs 22,各自内部一致 |
| 6 | 抽屉里的工具行仍 22 | 抽屉那两只格改前改后逐字节相同 |

### 消融(逐个撤掉一个机制,看恰好哪几条变红)

| 撤掉 | 变红的测试 |
|---|---|
| 链的选择器重新排除 `.thoughts` | 「思考那一格**没有被排除**在链外」/「思考那一格**在**步骤链上」 |
| 链退回只认 `.fold` + `:*-of-type` | 「链的选择器认**两种**行」/「不用 `:*-of-type` 判首尾」 |
| 线的 x 退回写死 14.5 / 7.5 | 「每一条线段用的是**同一个表达式**」 |
| `--chain-x` 与行首那一格脱钩 | 「两种线段落在**同一条绝对轴**上」/「链的中轴就是行首那一格的中轴」 |
| 顶层工具行退回 22px | 「顶层:三种行取同一个内边距」/「思考那一格的行首和工具行**取同一个值**」 |
| 思考那一格重新挂专属 29px | 同上两条 |
| 夹心正文那段线也留 25px 缺口 | 「缺口只留给行首那一格」 |

把 `record.module.css` 整份还原成改前的样子,这三个文件一共 **12 条**变红。

### 被推翻的那条测试

`thinking-embedded.test.tsx` 里原有一条「思考那一格不画步骤之间那条竖线 ——
它是工具行,不是步骤」,断言链的选择器必须带 `:not(.thoughts)`。它**没有被删,
改写成了相反的不变式**,并在测试里留下了当初排除的理由和推翻的日期 ——
「为什么曾经排除过」比「现在没排除」更值钱:下一个看到线和图标错开的人,
要先问「是不是又有哪一行的列错了」,而不是再把它从链上摘一次。

### 已知残留(未修,列出来)

- **`.tool` 与折叠头的 1px 差现在看得见了。** `.tool` 的图标→文字间距是 `gap: 8px`,
  `.fold > summary` 是 `7px`,于是同一列里工具行的名字在 **23**、折叠头的名字在 **22**。
  这个差 §F-15 就记着(当时是 45 vs 44),**不是这一轮引入的**;但这一轮把两种行
  挪进了同一列,**原来看不见的 1px 现在是相邻两行之间的错位**,比以前显眼。
  改它要动 `.tool` 的 `gap`,会波及壳外独立出现的工具行,另开一条。
- `.fold.flat > .body.stack > .code { margin-inline: 22px 11px }` 保持原样:
  它是「顶层的终端块」,22 是稿子给的、和这一轮的行首列无关(终端块没有行首格)。

## F-19 分叉带走结论,但已有的分叉不追溯(2026-08-27 用户裁决)

**问题**:fork 一个会话之后,原本成功的那一轮在界面上变成红色的「运行失败」,
而正文明明写着「已完成长篇深度文章页面」,产物卡也在。用户原话:「为什么 fork
之后, 之前成功的 run 会变成失败」。

**根因不是查不到,是复制的时候就没带。** `POST /api/projects/:id/conversations`
把 `runStatus` 和 `runId` 一起当成「run 指针」摘掉了。但这两个不是一回事:

- `runId` 是**把手** —— 重连、通知去重、SSE 续流都拿它认人,fork 出来的副本
  没有自己的 run,所以摘掉是**对的**。
- `runStatus` 是**结论** —— 那一轮到底成没成功。摘掉之后前端只能猜。

前端的猜法(`AssistantMessage.tsx` 的 `legacyTurnFailed`)是「events 里有任何
一条报错的 `tool_result` 就判整轮失败」。那一轮里恰好有一次
`desktop renderer unavailable`,于是整轮被判成失败。

真机逐字段对照(用户自己库里那两条):

| | 源消息 | fork 副本 |
|---|---|---|
| `run_status` | `succeeded` | **NULL** |
| `events_json` | 16886 字节 | 16886 字节(逐字节相同) |

**内容一个字节没变,只有结论没了。**

**修法**:只带结论,不带把手。终态(`succeeded` / `failed` / `canceled`)跟着
复制;`queued` / `running` 仍然丢掉 —— 那是源会话此刻还在跑,带过来会永远转圈。
`runId` / `lastRunEventId` 照旧摘掉。

### 已有的分叉会话不追溯 —— 选 A

老 daemon 建出来的副本,`run_status` 已经永久是 NULL,救不回来。三个选项摆给
用户之后,**用户选 A**:

- **A(采用)**:不管。新分叉正确,老的留着显示红色。零风险。
- **B(否掉)**:放宽 `legacyTurnFailed` —— 轮次已结束且有正文产出时不再因单条
  工具报错判失败。能一次性救回所有老行,**但会同时改变所有无 runStatus 历史
  消息的显示**,有些真该失败的会变成「已完成」。**否掉的理由:拿一个错的默认
  换另一个错的默认,而且新的那个会把真正的失败藏起来,比原 bug 更糟。**
- **C(否掉)**:结论缺失时画中性的「已结束 / 状态未知」而不是红色失败。最诚实,
  但是新的展示形态,要设计 + 19 个语言的文案,这一轮不做。

⚠️ **给下一个人**:如果你在老的分叉会话里看到「运行失败」但正文是成功的,
**那是已知的、已裁决不修的**。不要去放宽 `legacyTurnFailed` —— 那条路 2026-08-27
被明确否掉了,理由见上面的 B。要改只能走 C,而且需要产品重新拍板。
---

## F-20 耗时:壳头补上推理,思考那一格挂自己的数(2026-08-27 用户裁决)

用户真机四连,逐条对照真实 run 查过再动手。截图那一轮已经认出来了 ——
`.od/runs/4347efff-31d5-4322-b48f-a0b6f3ad24c9`,认得出来是因为清单那两条抽屉的
耗时逐字对上截图:`1m 46s` / `1m 11s`。

### ① 「耗时好像没有算进 thought 的耗时」—— 成立,而且差得很多

真因:壳自己的跨度 `shellSpan` **只由带时刻的事件撑开**。整条事件流里带时刻的只有
`tool_use.startedAt` 与 `tool_result.completedAt`;thinking 一个时刻都不带
(daemon 那边的载荷就是 `{ type: 'thinking_delta', delta }`,`PersistedAgentEvent`
里也只有 `{ kind: 'thinking'; text }`)。于是**第一个工具调用之前**和**最后一个
工具结果之后**的推理被整段切掉。

本机 `.od/runs` 里 38 条带推理的真实 run 逐条量过,**壳头无一例外少报**:

| run | 整轮 | 壳头(改前) | 少报 |
|---|---|---|---|
| `4347efff`(截图那一轮) | 6m 12s | **3m 11s** | 3m 0.6s |
| `3be1d04d` | 5m 54s | **1.4s** | 5m 52s |
| `9bbe3832` | 2m 17s | 0.4s → **屏幕上什么都不显示** | 2m 16s |
| `0dc3ba6a` | 11m 59s | 5m 42s | 6m 17s |
| `46ae3c0a` | 7m 5s | 1m 25s | 5m 40s |

修法是一条不变量,不是给推理另外补时间:
**开这一轮的那张壳从轮次开头开始走表,收这一轮的那张壳走到轮次收尾为止。**
`ensureShell()` 本来就是在本轮第一条事件上开的第一张壳(D10),最后一张壳一直开到
轮次终止 —— 那两个时刻是它俩自己的边界,不是借来的。单壳的一轮因此等于轮次跨度。
它同时**取代**了原来的 `turnElapsed` 兜底(那条是同一件事的特例),函数已删。

两张壳的那一轮仍然分得开:第一张拿轮次**开头**、最后一张拿轮次**收尾**,中间那道缝
谁也不领 —— T34 那张「两张卡头同一个数」的坏画面在结构上出不来。

被推翻的一条测试:`shell-elapsed-turn-span.test.ts` 的「事件给得出跨度时事件说了算
(它更窄,说的是这张壳不是整轮)」。前半句成立,后半句是错的:一轮只有一张壳时,
那张壳**就是**整轮。改写成相反的不变式,原文与推翻日期留在测试注释里。

### ② 顶层那三行为什么一个耗时都没有 —— 一半是 bug,一半不是

截图里顶层是 `Thoughts` / `ToolSearch` / `Thoughts` 三行,右边全空。分开看:

- **`ToolSearch` 那一行不是 bug。** 真实数据 `startedAt=1787828745434` /
  `completedAt=1787828745473`,**39ms**,低于 `UNKNOWN_ELAPSED_BELOW_MS`(100ms),
  按 §2.2b「调用与结果同批到达 = 不知道,不是跑得快」本来就该不显示。那一行是对的。
  顺带全量核过:624 条真实工具行里落在 (0,100ms) 这一档的是 **0** 条 —— 门槛没有在
  误伤谁;真正没有数的 259 条是**两端缺一端**(`tool_result` 没带 `completedAt`)。
- **两格 `Thoughts` 是真 bug**,见下一条。

### ③ 思考那一格的耗时怎么算

thinking 事件一个时刻都不带,「量它自己」这条路不存在。能观测到的只有
**它填掉了哪一段空白** —— 上一件带时刻的事结束到下一件带时刻的事开始。三条边界:

- 开头那一段:上一件事不存在时,起点是**轮次开头**(`startedAtMs`);
- 收尾那一段:下一件事不存在时,终点是**轮次收尾**(`endedAtMs`);**还在跑就不结账**;
- 空白里**不止它一个**(中间落过正文 / 工具行)时:一个数都不给 —— 那段空白是几件事
  分掉的,分给谁都是编。

**跨事件合并**(`groupThinking` 把连续几段收成一格):每一段各记各的空白,而相邻两段
的空白**共用同一个时刻端点**(隔在中间那次 `TodoWrite` 的 `startedAt` 既是前一段的
终点、也是后一段的起点),所以相加正好等于端到端跨度,不重复计、也不漏掉中间那一瞬。
一格里只要有一段算不出来,**整格不给数** —— 只加算得出的那几段是偏小的假数。

**正在想的时候不显示**,和进行中的 todo 是同一条规矩(`TodoRow` 的
`status === 'in_progress'` 那一档)。

覆盖率:本机 76 格真实思考里 **59 格(78%)** 报得出数;截图那一轮 8 格全报得出,
顶层两格是 `2m 34s` / `4.0s`,抽屉「交付前自检」里五格是
`5.4s / 4.3s / 42.8s / 7.0s / 8.9s`。各格相加 + 工具行相加 ≈ 壳头总数,对得上。

`ThoughtsRow` 原来的注释写着「不挂耗时:推理的时长在壳头的总耗时里」—— 那句话的
**前提就是假的**(见 ①),已改写。

### ④ 中止的一轮没有复制按钮

原判据只有一条:`message.content.trim().length > 0`。而推理走的是 `events`,从来不进
`content`(`ProjectView` 的 `textBuffer`:`kind === 'text'` 才 `appendContent`,
`kind === 'thinking'` 只落 `events`)。一轮被停在模型还在想的时候 `content` 就是空的。

新判据在 `runtime/chat/copyable-turn.ts`:**回答优先,回答一个字都没有时退回推理原文**。
两样都没有仍然不出按钮 —— claude 经 daemon 送出的 thinking 全是空串(真实录制 1786 帧
无一有字),给那种轮次一颗按下去复制空串的按钮比没有按钮更糟。

### 消融(逐个撤掉一个机制,看恰好哪几条变红)

| 撤掉 | 变红的测试 |
|---|---|
| 壳头的轮次边界 widening | `shell-elapsed-includes-thinking` 5 条 + `shell-elapsed-turn-span` 4 条 |
| `settleThink()`(不往条目上落耗时) | `thoughts-elapsed` 的 6 条正向用例(反向对照全绿) |
| `groupThinking` 不带 `elapsedMs` | 上面 6 条 + `thoughts-elapsed-row` 2 条 |
| `ThoughtsRow` 不把 `elapsed` 递给 `Foldable` | `thoughts-elapsed-row` 2 条正向用例 |
| 撤掉 `live ? null :` 抑制 | `thoughts-elapsed-row` **2 条反向对照**(正向全绿) |
| `closeThink` 的「仍是数组末尾」守卫 | 「推理和正文分掉同一段空白时,谁都不给数」 |
| `sumElapsed` 少一段就整格作废 → 改成部分求和 | 「一格里有一段算不出来时,整格不给数」 |
| `copyableTurnText` 不回落推理 | `stopped-turn-copy` 2 条正向用例(反向对照全绿) |

### 范围外发现(**未修**,列出来)

- **`daemonAgentPayloadToPersistedAgentEvent` 把 `tool_result.completedAt` 丢了。**
  落库那条链路只写 `{ kind, toolUseId, content, isError }`,而 web 的
  `translateAgentEvent`(直播那条)是留着的。结果:**历史会话重新打开之后,每一条
  工具行的耗时都没了**,壳自己的跨度也退化成「第一次调用开跑 → 最后一次调用开跑」。
  现有夹具 `amr-thinking-todo.turn0.json` 15 条 `tool_result` 全都没有 `completedAt`,
  正是照着落库那条链路导出来的。
- **`.od/runs/*/events.jsonl` 里每条 `tool_use` 都镜像了两遍**(同一个 `id`、相隔几
  毫秒、两个不同的 envelope id)。消息里落库的应该是去重之后的一条(用户截图顶层只有
  **一行** `ToolSearch`),但运行日志这一份会让任何直接回放它的工具看到重复行。
- **`tests/components/chat/thinking-fold.test.tsx` 在分支 tip 上本来就是红的**
  (「还在思考时不折叠」)。在 `od-wt-chat-panel` 上原样复现过,与这一轮无关。
- **`.tool` 与折叠头的 1px 差**:§F-18 已经记着,未动。
