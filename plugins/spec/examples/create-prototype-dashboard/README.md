# Prototype Dashboard

This is the full reference example for a spec create plugin. It includes a portable `SKILL.md`, OD sidecar, preview, seed asset, and eval file.

```bash
capt plugin validate .
capt plugin install .
capt plugin apply create-prototype-dashboard --input audience="support leads" --input topic="enterprise onboarding risk"
```

